# 白帝范围边界 · 烛龙 PRD 22 章逐章取舍

来源 PRD：《烛龙零信任访问控制系统 — PRD V1.0》（由深信服 aTrust v2.5.16 手册逆向推导）。
白帝沿用该 PRD 的零信任**访问控制**主线，对 UEM 与安全中心管理模块做减法。图例：✅ 保留并复用烛龙 · ➖ 部分裁剪 · ❌ 不做。

| PRD 章 | 模块 | 白帝 | 落地 / 说明 |
|---|---|:--:|---|
| S / 1 | 执行摘要 · 产品概述与定位 | ✅ | 复用；定位收窄为"零信任访问控制系统" |
| 2 | 安装与部署 | ✅ | 分离式 / AIO 形态复用烛龙引擎；console 无专页 |
| 3 | 系统架构与总体设计 | ➖ | 控制面/数据面分离原则不变；**网关横向扩展 + 客户端故障转移（FR-ARCH-03/04）已真实现**：剖面下发有序落点清单（在线优先→id 字典序，离线也下发）、指纹逐网关、客户端按序切换且切换在接入页可见，单数 `gateway` 字段保留供旧客户端。**控制面自身的多活/选主不做**（SQLite 单写者，双写会静默丢配置）；控制面侧改为**温备已真实现**（`baidi-standby` 周期拉加密备份 → 校验 → 落盘 → 回报，主机侧 mTLS `standby-` 前缀分权，系统页/`/diag` 三态真判定，切换脚本 `deploy/promote-standby.sh` 且干跑有用例跑）——RPO = 同步间隔、切换需人工触发、不做自动选主（两节点无仲裁必然脑裂），**就近选择与负载均衡刻意不做**（没有地理/时延数据；心跳指标无调度语义，按 CPU 选最闲会把所有新连接一起打到同一台）；边界见 docs/ARCHITECTURE.md 第七节 |
| 4 | 产品升级管理 | ➖ | 升级前校验（版本序/禁降级/强制链路/组件一致性/包签名）、加密配置备份；**客户端灰度 = 服务端判定真实现 + 终端提示闭环**：控制面按 SHA-256(平台\|账号) 稳定分桶 + 定向账号名单，且只在目标版本**高于**终端上报版本时才回 `update=true`（相等或更低不提示——那是降级）；**终端两端都接了最后一跳**（桌面端登录/重开即查、移动端「我的」页进入即查 `GET /client/update`，有新版出横幅指向门户下载中心，检查失败静默不报错——后端按 platform 分桶本来就支持 android/ios/harmony，此前只是移动端从没调过）。**灰度链已收尾（wave8 行动 15）**：按用户组定向在控制台可配可回读（此前弹窗保存恒发 `groups: []`，管理员只要在页面上改一下比例就会把经 API 配过的用户组无声清空——**一个会吞配置的页面不属于"已知限制"，那是缺陷**）；`upgrade.Coverage` 接进升级页，每条计划显示**精确**命中人数（分桶确定性，不是 `accounts×percent/100` 的估算），读取失败显示「—」而不是 0；新增**现场终端实际版本分布**（`posture_reports` 按 (平台,版本) 聚合，「未上报」单列一桶），那是 AC-12「先小范围验证再放开」在真机上的唯一验收依据。**仍不做**：客户端不自动下载、不自动安装、不自动回滚，装包一律人工去下载中心取；灰度只影响"告诉谁有新版"，不影响任何人**实际**装了什么版本（唯一权威事实是 posture 上报的 `client_version`）。服务端换二进制仍由部署脚本执行，集群升级编排未做（集群本就没部署）。**升级包签名链路已补齐（wave9 行动 18，FR-UPG-04）**：新增发布方离线 CLI `control/cmd/baidi-upgrade`（`-genkey` / `-manifest` 自动算 SHA-256 / `-sign` / `-verify`），与 `baidi-license` 同一条分界——私钥留在发布方，控制面只配 `BAIDI_UPGRADE_PUBKEY`。改造前验签是 fail-closed 的（方向正确），但**全仓没有任何签名工具**、公钥也没进任何部署模板，于是升级包校验在**任何真实部署上恒为「校验不通过」**；公钥已进 `config.env.example` / `deploy.sh` / `install-remote.sh`，升级页在未配置时**预先**置灰校验入口并说清怎么配 |
| 5 | 监控中心 | ✅ | `MonitorDashboard` 安全监控大屏；**闲置账号治理已补齐（wave9 行动 7，FR-MON-19）**：策略落 `settings`（`store.IdlePolicy`：阈值 + `autoLock`，出厂 90 天且**不自动锁定**）+ `GET/PUT /users/idle/policy` + `StartIdleLockLoop`（默认每小时一轮，`BAIDI_IDLE_LOCK_INTERVAL`）。改造前阈值只从 URL 参数取（刷新一次就回到写死的 90 天）、`autoLockEnabled` 整项不存在，于是 PRD 验收那句「若开启自动锁定，Then 该账号被自动锁定」**没有任何执行方**。三条纪律：自动锁定与手工批量**共用 `lockIdleAccount` 一条路径**（含防自锁、管理员目标闸、数据面撤窗，「自动」不是绕开守卫的第二条路）· **永不处置管理员账号**（后台没有调用方可比对 `PermAdmins`，一个能自己锁管理员的定时任务最坏能把系统锁到没人登得进去）· 管理员跳过**不逐条记审计**（每小时一轮，一个长期不登录的审计管理员就够把审计冲成噪声）。**「在线」口径同批统一**（FR-MON-14）：用户状态页此前算的是 `posture 上报在 10 分钟内`——那是**采集器还活着**，不是"此刻连着隧道"，与「在线用户」页的真实会话是两回事，两个数并排显示在同一套控制台上。**用户状态页补齐两项操作（wave9 行动 20）**：爆破锁定 IP 的**批量解锁**（FR-MON-18 P0 原文逐字写着「或选中单个/多个后批量解锁」，此前只能一条条点，而一次内网扫描就能产生几十条）与受关注用户的**关键词检索**（FR-MON-15；此前只能靠肉眼在长列表里找，而这一页的定位就是「就近处置」） |
| 6 | 用户与角色（身份目录） | ✅ | `IdentityUsers/Org/Groups`；**外部目录组映射已真**（LDAP memberOf / OIDC groups → kind=external 用户组，按登录逐次收敛、双源隔离、页面只读，直接进 `SubjectIndex` 供资源授权与认证策略引用）；目录全量同步为 L 级延后项（见 docs/charter/wave7.md D 组）；**明示不做**：自定义 HTTPS 认证目录（FR-USER-05）——私有认证服务器没有稳定 Subject，绑定只能退回按用户名，那正是本项目在认证源实现里指认过的冒充漏洞（AD 里新建一个同名 admin 即可冒充本地管理员）；企业微信/钉钉/浙政钉/飞书目录连接器（FR-USER-08 后半）——依赖外部平台租户与实机验证，本环境无法诚实交付，标准路径是这些平台的 OIDC 出口（已可用）。**wave9 三处收口**：①**目录四列脱冻结值**（FR-USER-03/FR-MON-04）——`online/risk/device/ip` 全仓无 UPDATE（`grep -E "UPDATE users SET (online\|risk\|device\|ip)"` 零命中），显示的是**建号那一刻**的值：种子里的张伟从库建好那天起就一直是「在线 · Windows 11 · 10.8.2.31 · 无风险」，哪怕一次都没登录过，而真正接入的人永远显示离线；现改为按真实信号现算（会话与 `/online` 同源、posture 最新上报、`riskOfAccount` 与在线会话页同一处判定），取不到一律降级成 `—`/`unknown`，**绝不保留库里那个陈年好值**——从未上报过环境的账号显示「无风险」是替一台完全未知的机器背书。②**认证方式按实算**：`users.auth` 是一列零消费方的自由文本，种子填的是「SAML SSO / 密码+UKey / 密码+短信」——白帝一种都没实现，而页面把它当事实展示、建号表单还让人从这几项里挑；改为由口令来源（本地/外部目录）+ 真注册过的 passkey/TOTP 派生。同一条纪律早就用在**管理员**那张表上（`admins_sqlite.System` 的注释写得很清楚），只是没做到用户目录这半边。③建号撞已存在账号回 **409「账号已存在」**（此前落到 `orgStoreErr` 的 default 分支 → 英文 500「failed to create user」，而同一套控制台的 CSV 导入对同样的输入回的是「账号已存在」，两条路两种解释）。**wave9 第二批三处（行动 12/13/14）**：①**身份源选项卡真过滤**（FR-USER-01）——`DirUser` 补 `SourceID`，判据与选项卡计数同源；此前那排选项卡是纯装饰的，卡上写着「总部 AD 域 3」点进去却是全量表。②**账号可改**（FR-USER-02）——新增 `PUT /users/{id}`，只收姓名与邮箱；**account 显式拒改**（令牌主体 + 多张表的关联键）。③**账号可删**（FR-USER-15）——新增 `DELETE /users/{id}` 与 `…/delete-preview`：这是 License 席位的**唯一**释放路径，而在此之前 `license.go` 的 409 文案与闲置治理弹窗都指向一条不存在的路。删除做账号维度连带清理，**刻意不动** `resources.allow_users` 与 `jit_grants`（前者是安全授权、清掉就再也看不出原来授权过谁，后者要留给审计），两者写进影响面回执由人去清 |
| 7 | 认证管理 | ✅ | `IdentityAuth/AuthPolicy/Idp/PwdPolicy/SecPolicy/Waiver`；**外部账号状态回验已真，但覆盖面按目录类型分三档，别笼统说**（wave8 行动 11 校准）：**AD** = 禁用（userAccountControl 位）/ 过期（accountExpires）/ 删除 / 移出 BaseDN，四种都识别，无需配置；**通用 LDAP** = 删除 / 移出 BaseDN 恒识别，「禁用」**要配了状态属性映射才识别**（`statusAttr` + `statusDisabledValues`，控制台给了 IDTrust/389DS/OpenLDAP ppolicy 三个预设）——LDAP 协议里没有统一的「禁用」语义，不配就只剩存在性这一种证据，页面当面告警；**OIDC** = **没有周期回验**，OIDC 协议不提供状态查询通道（那是 SCIM 的职责）。OIDC 账号在**每次登录**时由 IdP 天然重新校验（被停用的人完不成授权码流），缺的只是**会话中途**吊销，窗口上界 = 会话有效期。刻意不做 refresh_token 轮询：那要把一把能替用户向 IdP 换令牌的长效凭据落到本地库，库一旦泄露，攻击面比它想解决的问题大得多。要覆盖中途吊销请缩短会话有效期，或给同一目录再配一条 LDAP 源做回验。三档共同的纪律：源不可用（网络/绑定失败）绝不动手，只单向禁用不自动恢复；**明示不做**：LDAP 手机号字段映射（FR-AUTH-08 子项）——短信网关已明拒，手机号在系统内没有任何消费方，映射进来即孤儿数据。**认证策略抽屉同批收口（wave8 行动 13-②③）**：删掉两端的「主认证方式」下拉（策略按目录筛，对已被该目录认出来的人说"主认证用证书"是同义反复）与「默认授权应用」（零执行方），合并 PC/移动端两栏（三端走同一个 `/portal/login`，请求里没有端标识），并给 `secondary` 一个真执行方——列了方式的策略**不接受 legacy 演示验证码回落**，裸 IP 演示站正是它唯一生效的地方；**本地口令找回（自助重置）明示不做**（wave9，FR-AUTH-06 后半）：PRD 形态是「结合短信认证服务器验证身份后重置」，而短信网关已明拒——没有第二因子就没有安全的自助重置。替代路径现成：管理员重置口令 + 首登强制改密。**多认证域下的爆破记账已收口（wave9 行动 5，FR-AUTH-09 + FR-MON-14/17）**：`routeDirectory` 在「外部源 ≥2 且未指定 directory」时回 ambiguous，门户登录这条分支**直接 return**——不计锁定、不落审计。豁免的本意是放过"合法外部用户忘了选域"，但它对**本地口令账号**同样生效：接了两个外部源之后，对 `/portal/login` 反复提交 `{username: admin, password: 猜的}` 且不带 directory，每次都从那里 return，而口令一旦**猜对**代码根本不进这个 if 块、直接登录成功——响应差异构成一个干净的口令预言机，整段爆破在「用户状态」「爆破锁定 IP」两张表与审计中心里恒不可见。收口后的**不变式**：一次输错的本地口令算不算数，**不该随部署接了几个认证源而改变**（判据 `found && cred.PassHash != ""`，外部绑定账号 pass_hash 恒空，精确保住原豁免，有反向用例钉住） |
| 8 | 应用管理（资源发布） | ➖ | 隧道应用发布/资源鉴权/JIT 审批已真实现；**七层 Web 代理（8.3.3）已真实现**（控制面签 use=web 短时效一次性票据 → 网关 L7 验票换会话 Cookie → 逐请求重新鉴权 → 反代；XFF 按真实对端重写、Location/Set-Cookie 作用域改写、`web-e2e.sh` 九条断言，含票据重放、跨应用 Cookie、撤权后立即失效三条反例）。仍不做：**HTML 正文绝对链接改写**（无底洞，如实标边界）、**DLP/水印/禁复制打印下载**（属 ch11 UEM，整章不做）、**SSO 免认证代填**；L7 端口不受 SPA 隐身保护，边界见 docs/ARCHITECTURE.md 第七节。**应用可改可删已补齐（wave8 行动 14，FR-APP-01 P0）**：`PUT/DELETE /api/v1/apps/{id}`——改造前只有 GET+POST，而控制台「编辑」走的是发布向导 → POST，点一次多出一条同名应用；下架**不级联删受控资源**（可能被别的应用引用、也可能挂着 JIT 授予），回执与确认弹窗都当面说明。顺带摘掉 `apps.node`（管理员没有这个输入项，一律写死「华东出口」）。**「WEB 全网资源」正名为「直连书签」**：`mode=global` 不经网关、不受访问控制、对全体登录用户可见，三处名字统一 + 向导与编辑抽屉各挂告警 + 门户打开行为改对（此前提示「请用桌面客户端接入后访问」，而它压根不走隧道）；**泛域名代理（证书签发 + 正文改写）仍不做**；**⚠️ 对象库与资源侧的能力错配**（wave9，FR-TUN-02/03，均 P0）：对象库把 CIDR / 地址范围 / 域名通配符、端口列表 / 端口范围都当**一等对象**提供并做成种子，而资源侧只接受单个 `host:port`——照着对象库选两个合法对象，自动回填出的 backend 是网关拨不出去的字符串。wave9 已给 `handleSaveResource` 补**完整**形态校验（`validateBackendHost`：网段 / 地址范围 / 泛域名逐类拒收并**说得出正确形态**——实测改造前这三种全都 200 OK 落库过，而网关按它 `net.Dial`，症状是剖面里有它、客户端点开就是连不上、两侧日志都不报错），但**对象库那一侧的能力差距仍在**（那三类对象仍可建、只是不能直接当发布目标） |
| 9 | 终端管理 | ✅ | `IdentityDevices` + `IdentityCompliance`（终端合规基线 = 设备 posture，属身份/设备信任，**保留**）；**资产分类与标签（FR-EP-06~09）已真实现**：`asset_class`(企业/个人/企业纳管个人) 是准入判据，执行方落在敲门令牌闸的 (账号,指纹) 粒度上（`personalPolicy` = 跟随全局/按严格判/一律拒，`managed` 按企业资产处理）——**刻意不并入账号维度的风险降权**，否则同一个人的企业机会被他的个人机连坐；`tags` 只是台账属性、无执行方且页面当面标明。**边界**：真正的「按设备区分能访问哪些资源」需要把指纹贯穿数据面（撤销通道与网关 DenyUsers 现在都只有账号维度），本波只做到"这台设备能不能拿到敲门令牌"；**记档延后（非否决）**：终端日志远程收集（FR-EP-17/18/19）——需要新造服务端→客户端的指令下发通道（现架构客户端只拉不收），改造面大于收益；桌面端本地一键诊断报告落地后价值进一步降低 |
| 10 | 策略管理（全局/用户策略） | ➖ | **接入策略两条 P0 已真实现**（wave8 行动 13-①）：FR-POLICY-29 同时在线设备上限（0~1000，0=禁止接入，可区分 PC/移动分别计数，名额先到先得所以调小上限会真的把最晚接入的挤掉）与 FR-POLICY-30 接入超时注销（无**业务流量**超时，5 分钟~365 天）。执行点是**敲门令牌**（`api.accessSessionGate`），撤销在一个 15s 保活周期内生效；活跃时刻来自网关逐会话回执（`spa.Allowlist.Touch` → 心跳 `lastActive`），**敲门保活刻意不算活跃**，否则这条规则永远不会触发。防爆破（FR-POLICY-06/07）真实现见 ch7。**摘除的**：「用户策略 · 继承编辑器」那 8 项（设备并发数/会话空闲超时/专用 DNS/虚拟专线隔离/登录时段/二次认证豁免期/卸载防护/进程防护）——它们落进 `policy_overrides.settings` 后**全仓零消费方**，而保存提示写着「已下发至代理网关」，影响预览里的平台分布是 `members×0.62/0.16` 编的。**不做**：按组织/用户组分级的策略继承（FR-POLICY-02~05，接入策略当前是全局的）、PC 端「无键鼠操作」超时（控制面拿不到终端输入事件）、卸载/进程防护（需驱动或系统服务）、弱网/0RTT 优化、禁止浏览器登录、强制安装/升级客户端——逐条理由列在策略页「本版本未实现」里。**PolicySimulator / LoginFlowSim 从未实现**，规划页未建 |
| **11** | **UEM 统一终端数据安全** | ❌ | **整章不做**：移动/PC 数据安全、沙箱工作空间、外发审批等 DLP 移出范围 |
| **12** | **安全中心** | ➖ | **安全基线管理**：**保留并真实现**（此前这一行写着「砍」，与代码相反——`console/src/views/Security.vue` 带完整 CRUD，且 `baseline_policies` 是风险引擎 `internal/risk` 的**唯一规则源**，砍掉它等于终端合规判定没有规则）。基线的检测项 key 受采集器目录约束、`disposal` 四档都有执行方、**适用范围（wave8 行动 13-④）已结构化并真接进判定**（`scopeOrgs`/`scopeGroups`，组织含子树，与资源授权/认证策略共用 `store.SubjectIndex`；两栏都空 = 对全体生效）；同批摘掉无人读的 `type`（上线准入/应用防护）与自由文本 `scope`。**真砍掉的是**：虚拟网络域 / 可信应用 / 独立的终端基线管理页（终端合规并进 ch9）。**SPA 服务隐身保留**为安全代理网关内建能力（隐身价值主张底座，烛龙 console 本就不暴露专页，天然对齐），其真实态回执见 wave8 行动 7。**「攻击面 = 0」这句断言已把 L7 敞口算进去（wave9 行动 11，FR-SEC-SPA-02/05/12）**：控制面一直收得到网关自报的 `web` 监听地址，只是没人读，于是一台开着 `-web` 且 nft 规则装好的网关，隐身页会同时显示「端口扫描全程超时，无任何端口可探测」与「攻击面 = 0」，而 nmap 对着 18444 一扫一个准。`GatewayBundle` 现补 `webExposed`/`webEndpoints`，`allArmed` 加第二个前提，并出一条「B/S 免客户端接入与端口隐身是一组取舍，不能同时成立」的告警——这是这一页唯一一句**正向安全断言** |
| **13** | 深信服设备联动与第三方设备管理 | ➖ | 深信服专有联动（EDR/AC 私有协议）不做。**「保留开放 API/标准化第三方联动（见 ch21）」这句原来是个空引用**——ch21 那一行当时是个盖住整章的 ✅，于是 FR-INT-12 既不在 ch13 也不在 ch21 的账上。如实说：**第三方风险摄入今天没有任何合法路径**（`POST /posture` 第一行就 `requireUser`，mTLS 那几个口按 CN 前缀只给自家进程），定性为**记档延后（M）**，最小可接受形态是照 notify webhook 的先例做反向摄入端点；边界与理由见 ch21 行 |
| 14 | 主动防御（欺骗防御 / 恶意识别） | ➖ | **仅处置末端真实**（强制下线/账号冻结/JIT 撤销真落数据面）；安全雷达/蜜罐诱捕/恶意识别未实现，规划页未建 |
| 15 | 系统管理 | ➖ | 三权分立 / 消息通道 / 日志外送 / 加密配置备份 / 运维诊断均已真实现；**HA（15.5）落成温备而非双活**：备机拉加密备份+校验+落盘，不提供服务，切换由 `deploy/promote-standby.sh` 人工触发，RPO = 同步间隔。**时间一致性已真实现**（网关心跳自报本机时钟 → 控制面收包时刻做差 → 网关页三态呈现（数值/未上报，绝不把未上报当 0）+ `clock_skew` 告警规则 + `/diag` 时钟检查，阈值挂着敲门令牌有效期这个真实临界点；**NTP 客户端配置刻意不做**——时钟同步由部署机chrony/ntpd 自治，控制面只做检测与告警，代管 NTP 等于给控制面加一个能改全网时间的写权）。**运营报表已真实现**（`GET /api/v1/audit/report`：audit_log/alerts 的 SQL 聚合——逐日分桶补零日、留存截断如实标记、登录/被拒账号榜单、告警三档；**归 PermAudit** 且挂在 /api/v1/audit* 前缀下——报表是聚合过的审计正文，聚合并不脱敏，给 security/system 开这个口等于三权分立只剩字面成立；页面无演示数据，连不上如实说）。**License 已真实现**（Ed25519 发行签名：发行方离线 CLI `baidi-license` 持私钥，控制面只配公钥 `BAIDI_LICENSE_PUBKEY`、未配置即拒导入；容量执行点收在建号（/users、/admins 建号分支）与签发网关证书两处硬闸，换证/提权/吊销不误占席位；demo=不限、invalid/expired=fail-closed 拒新增；外部认证源建号刻意不硬拦——拦在登录链路是随机拒绝服务，超限走状态页亮红；**不是防拷贝**，边界如实写在页面上）；**15.3 / 15.4 / 15.6 / 15.9 四小节与 FR-ADMIN-05~10 明示不做**（wave9 正式落表，wave8 建议 6 已判）：15.3 终端个性配置与 15.6 特性中心是形态差异；15.4 网络部署与 15.9 区域/虚拟 IP 池是**设备形态需求**（白帝是软件进程，且业务侧看到的源 IP 恒为网关，用户维度溯源在业务侧结构上不可得）；管理员的**内容分权**（管理范围 / 17 级树）不做——白帝的分权是按权限键的三权分立，不做"按组织子树切分管理范围"那一维（漏掉任何一个写端点都是静默越权，收敛的模型比宽而漏的安全）。**控制台接入 IP 限制（FR-SYSCFG-08，P0）记档延后**：刻意不做成应用层开关（那只挡得住已走到应用的请求），正确位置是前置 nginx / 防火墙，`deploy/nginx/baidi.conf` 现已给出注释掉的 `allow/deny` 样例。**wave9 三处收口**：①**passkey 的 helpdesk 通道**（FR-ADMIN-17/20）——passkey 没有恢复码、本人删不掉最后一个（那道守卫的前提是本人还能登录）、而 `secondFactor` 规定「已注册 passkey 即无条件强制断言」，三条合起来 = 认证器一丢**永久登不进来**，而管理员侧此前连一个端点都没有（TOTP 早就有）；新增 `DELETE /users/{id}/passkeys`，与 `handleAdminResetTotp` 逐字对称（`PermSecurity` + `guardAdminTarget`）。②**管理台补二次认证注册入口**（FR-ADMIN-16/17）：系统管理页专列一栏「二次认证」却持续显示「未注册」，而管理员在整个管理台里找不到地方去注册（注册页 `/portal/security` 一直存在，只是没有链接到得了）；同批把那一栏从写死的「已注册 passkey」改成读**机读因子清单** `AdminAccount.Factors`（只绑 TOTP 的管理员此前被显示成绑了 passkey）。③**防爆破配置的权限发错了人**（FR-POLICY-06/07）：`PUT /security/lockout-config` 挂在 `PermSystem` 上，而同一文件的解锁、同一页面的接入策略、权限键自己的中文定义三处都指向 `security`——失衡是双向的，且方向是**削弱认证防线**（系统管理员能静默关掉全站登录防爆破）；已改为 `PermSecurity` 并进 RBAC 矩阵测试。**wave9 第二批三处（行动 15/16/19）**：①**自定义角色可改**（FR-ADMIN-04）——后端一直是 upsert，控制台只用了一半，而角色有成员时删不掉，于是「收缩某角色权限」在页面上无路可走；同批给「新建」加撞 key 守卫（那个端点是 upsert，直接提交会静默改掉现有角色并**立即**生效）。②**外部目录账号不得提升为管理员**（FR-ADMIN-01/03/16）——提权入口此前不看口令来源，提上去 200 OK、管理员表里一应俱全，而他**永远登不进管理台**（外部账号 pass_hash 恒空，管理台登录没有认证域路由），且每试一次都被记成口令错误并计入防爆破；选**收敛**（与 FR-ADMIN-16 一致），两条提权路径共用一道闸并说清补救路径。③**主机侧定期自动备份**（NFR-AVL-04）——`BAIDI_BACKUP_DIR`/`_PASSPHRASE`/`_INTERVAL`/`_KEEP`，原样复用 `upgrade.CreateBackup`、临时文件 + rename 原子切换、备份 0600、按文件名而非 mtime 清理；**未配置时 `/diag` 判 warn 而不是 skip**——「没有任何自动备份」是每套部署都该关心的事。改造前 `CreateBackup` 只有两个非测试调用方（手点导出直接 stream 给浏览器、备机来拉时现造），单机部署一份自动备份都没有 |
| 16 | 审计中心（日志） | ➖ | 写操作实时落库已真（45+ 审计点，admin 门 + XFF 信任边界）；HMAC-SM3 防篡改链（/audit/verify 全链校验）、留存轮转（BAIDI_AUDIT_RETENTION_DAYS，链锚点接续）、CSV 流式导出均已真；**日志外送已真**（RFC 5424 syslog over TCP/TLS + HTTP JSON，持久化队列 + 退避重试 + 队列上界与丢弃计数，外送带 seq/mac 供 SIEM 独立验真；刻意不做 UDP）；**明示不做**：解密流量旁路镜像（FR-AUDIT-16/17）——那个形态依赖硬件化网关与专用镜像口，与白帝的进程形态不匹配；SIEM 深度审计的需求已由带 seq/mac 的审计外送承接（SIEM 侧可独立验真，那是旁路镜像给不了的）；**FR-AUDIT-05 的「设备出向行为分析」三缺一**（wave9）：账号分析与设备入向可做，**出向四元组检索不行**——`store.AuditEntry` 没有目的地址/端口/源端口字段，`SearchAudit` 也没有对应维度，数据面安全事件只带源 IP 与类别，目的地址只出现在自由文本里。补它需要给审计条目加结构化四元组列并贯穿网关上报链路。**wave9 三处收口**：①**审计类别字典收敛到 `store.AuditCategories` 一处**——`policy` 有四个真实写入点（保存安全基线 / 接入策略 / 网关接入地址），却**写得进库、任何一张类别卡都数不到、按它筛直接 400**（清单散在四个地方各抄一份，都漏了它；实测导出 39 行里有 1 条 policy，而卡片合计 38）；现列表聚合 / 检索校验 / 导出校验 / 态势总览四处同源，并由 `TestAuditCategoriesCoverWrites` 扫源码写入点钉住。②**检索与导出共用 `auditWhere`**——导出此前自己拼了一份 WHERE、只认 category/from/to 三维，账号与源 IP 两维压根传不进去，而页面上刚筛过的正是那两维：屏幕上筛出 12 条、导出的 CSV 里是全表，而管理员会以为这份 CSV 就是他刚看到的那些行拿去交差；导出弹窗同批改成**继承屏上当前筛选**。③补源 IP 检索维度（后端 `SearchAudit` 一直支持、页面没有入口，而 wave8 行动 8 刚为「按攻击源可检索」修过 `src_ip`），并修掉时间快选的 `toISOString()` 时区错位（UTC+8 每天 00:00–07:59 查的是昨天）。**外送失败有了主动信号（wave9 行动 17，FR-AUDIT-14/15）**：新增第 14 条告警规则 `audit_forward_fail`（判据是「多久没**成功**过」而不是「上一次失败了」——瞬时失败会被退避重试救回来；队列积压单独报一条，那是**不可逆**的）+ `/diag` 的 `audit-forward` 项。改造前失败路径只有一行 `slog.Warn` 与系统页里那一格，合规链路可以**静默断掉**：审计照常落本地库、页面一切正常，而 SIEM 那边从某一刻起再也没收到过东西 |
| 17 | IPSec VPN 组网 | ✅ | `GatewayIpsec` 站点编排（复用烛龙 IPSEC 引擎）。**站点 `peer` 只接受 IP / IP:端口**（wave8 行动 17）：组网网关刻意不解析 DNS（对端在 DNS 抖动时切换落点会造成谁也解释不清的间歇性故障），而入口此前放行域名、400 文案还主动推荐 `sh.example.com`——管理员照填拿 200 OK，站点安静地永远 down |
| 18 | 地址转换（NAT） | ✅ | SNAT/DNAT 策略建模 + 校验 + 编译成内核 nft/pf 规则并下发；隧道/敲门流量自动排除、SPA 互斥当面告警。真实报文转换需 root + 多网卡实机，未实测（同 darkfw 边界） |
| 19 | 典型部署场景 | ➖ | 文档级为主。多数据中心那一半的**客户端侧**已真实现（多网关落点 + 故障转移，见 ch3）；跨中心的统一入口/智能 DNS 就近解析不做 |
| 20 | 非功能性需求（NFR） | ➖ | **这里原来是一个 ✅ 加一句「安全/性能/可靠/信创/合规，贯穿」——一个对勾盖住 34 条 NFR，与 ch21 是同一个结构性缺陷（wave9 逐条核实后拆开）。核实结论（核实时点）：34 条里真实现 7 条、部分 19 条、未实现 8 条，其中 NFR-PERF 整族六条最弱（核实时四条无任何测量点）。★同批已把 **PERF-03 从 ❌ 改造成 ➖**（外部认证预算 + 耗时埋点，详见下方 PERF 段），故当前计数是**真 7 / 部分 20 / 未实现 7**；PERF 族现在是**两条**完全无测量点（PERF-04 策略下发时延、PERF-05 扩容），PERF-01/02 有了进程内基准但**仍不能声称达标**（有测量点 ≠ 达标，见下方 PERF 段与 ARCHITECTURE 第七节）。** **★通则（wave9 修订）：本项目**无压测**，四个 e2e 脚本全是功能断言。凡验收方式为「压测达标 / P95 ≤ 阈值 / 渗透测试 / 兼容矩阵」的条目一律没有交付物，别按已完成记账。例外**两处**、定位都是**防回归不是容量承诺**、口径都写在文件头：① `control/internal/store` 的四组 Go benchmark（wave9 新增，`SubjectIndex`/`Users`/`BlockedAccounts`；测的是一次库调用而非一次请求、modernc 纯 Go SQLite、热库）；② gateway 数据面——`spa`/`knock`/`resource` 三组纯内存微基准 + `internal/proxy/bench_test.go`（wave9 第二批：每流 TLS/TLCP 握手成本 / 单流吞吐 / 并发建流与到顶行为，全部进程内回环、无 RTT、上限是注入的小值），口径与本机样本见 ARCHITECTURE 第七节 PERF 段。**仍然连测量点都不存在的**：任何 P95、多流聚合吞吐、真实网络下的时延分布、`dataplane` 的 utun/netstack 那一段、策略下发时延。** **SEC（8）**：SEC-06 防爆破 ✅、SEC-07 三权分立 ✅ 两条真；SEC-01 最小暴露面 ➖（SPA 敲门真、内核态隐身有八态实测回执，但**参考部署默认不开 `-pf`**，此时未敲门的 TCP 会完成三次握手再被 accept-then-close 断开，nmap 判 open；控制面不做外部扫描，"端口全闭"未经外网侧实测）；SEC-02 全链路加密 ➖（TLS/TLCP 真，未抓包验证）；SEC-03 密钥存储 ➖（bcrypt + AES-256-GCM secret 盒真；`BAIDI_JWT_SECRET` 的默认值是编译进二进制的常量，仅逃生舱路径用得到）；SEC-04 国密 ➖（TLCP 隧道 SM2/SM3/SM4 真，**IPSec 的 `suite=gm` 是 IANA 私有码点、不是国密 IPSec**；未过密评）；**SEC-05 默认安全开局 ➖ 而非已收口**——首登强制改密与"无默认弱口令"两句是真的（wave8 行动 16），但 PRD 原文的第三句「**管理通道独立**」并未实现：管理 API 与门户/控制台共用 `:8090` 一个监听、一份 nginx 站点，没有独立管理口也没有管理网段限制（`deploy/nginx/baidi.conf` 里那段 `allow/deny` 是注释掉的样例，且只挂在管理员登录端点上——控制台与门户共享 `/`，整站限源会把普通用户一起挡掉）。与 FR-SYSCFG-08 同一条理由：这属于网络层收敛，产品不另造一层假开关；**SEC-08 组件加固 ❌**——无渗透测试、无依赖漏洞扫描（govulncheck / gosec / CodeQL / npm audit / trivy / dependabot 全仓零命中）、无任何安全响应头（CSP / HSTS / X-Frame-Options / X-Content-Type-Options 全仓零命中）；`BAIDI_CORS_ORIGIN` 默认 `*` 且 `deploy.sh` 把它带进生产装机（wave9 补了白名单能力 + 控制面启动告警 + 装机脚本复述，**默认值刻意未收紧**：客户端 webview 的 origin 逐平台不同而只实测过 macOS 一个平台，漏一个 = 那个平台升级即全员连不上，理由写在 `httpx.CORS` 注释里）。散点式的正确做法（Bearer 而非 Cookie 故无经典 CSRF 面、L7 Cookie HttpOnly+Secure+SameSite、XFF 先剥再重写、前端无 v-html）是逐条设计决策的副产品，不构成"按 OWASP Top 10 加固并验证"。 **PERF（6）· 整族最弱**：PERF-01 并发规格 ❌（无规格表、无压测；`proxy.maxConcurrent = 1024` 是防 OOM 的常量不是声明过的规格，且它的注释一直写作"握手/前导阶段上限"而实际覆盖整条会话寿命——wave9 已纠正注释，并把"到顶时静默挂住 accept 循环"改成拒绝并留痕 `proxy-capacity`）；PERF-02 吞吐/新建连接速率/转发时延 P95 ❌（**单流吞吐**与**每流完整握手成本**已有进程内基准——`internal/proxy/bench_test.go`，是量级不是规格，数字见 ARCHITECTURE 第七节；**P95 与聚合吞吐仍无测量点**，故仍不能声称达标；`gateway_metrics` 的 `RxBps/TxBps` 采的是**宿主机全机非回环网卡之和**，不是隧道流量）；PERF-03 认证时延 ➖ **wave9 已改造**（改造前：零埋点 + LDAP 缺省 5s/10s 且 api 层从不设，而 `RequestTimeout` 是**逐请求**的、一次认证走两次拨号 + StartTLS + 三次操作，最坏约 **60s**——此前这里写的「~15s」是低估。现在：逐请求超时按 ctx 剩余预算折算，外部认证套 8s 预算（`BAIDI_EXTAUTH_TIMEOUT`），耗时进 slog 与审计正文。**刻意不给 handler 加整体 deadline**：它压不住 go-ldap（拨号与请求都不吃 ctx），却会让 deadline 过期后的审计写入、锁定落库、认证策略判定一起失败——那是把「目录慢」升级成「全员登录不了」，有对照用例钉住。**OIDC 已同批覆盖**（`AuthURL` 补 ctx 参数 + `Exchange` 套同一预算；并修了两个非超时的真缺陷：三类错误此前被塌缩成统一 deny、Provider 每请求新建导致缓存与降级双双失效）。**仍不能声称达标**：无 P95 统计、无压测；bcrypt 不吃 ctx 打不断）；PERF-04 策略下发时延 ➖（真链路是网关 30s 轮询，"≤ 数秒"达不到，但方向确定、无计时验证）；PERF-05 横向扩展 ➖（多网关与客户端故障转移真，见 ch3；控制面仍是单写者，无扩容压测）；PERF-06 大规模控制台 ➖ **半边已补**——检索那一半 wave9 修完（Users/Resources/Apps 三页的搜索框此前是纯装饰的 div，审计页写死 `limit=200` 且从不发 offset 导致第 201 条之后不可达，现均已接线，并加了构建期守卫 `check-dead-ui.mjs`）；**服务端分页仍未做**（`/users`、`/resources`、`/apps`、`/devices`、`/orgs`、`/groups` 六个端点无 LIMIT，前端全量渲染无虚拟滚动），记档延后见 ARCHITECTURE 第七节。 **AVL（5）**：AVL-05 设备巡检 ✅ 真；AVL-04 配置备份恢复 ➖ 接近真（加密备份 + `VACUUM INTO` 一致性快照 + 温备周期拉取校验 + `promote-standby.sh`，**但没有主机侧的定期自动备份调度**，PRD 那句"定期自动"只有备机拉取这一路）；AVL-01 可用性 ≥99.9% / 无单点 ➖（数据面多落点真；**控制面是单点**，温备 RPO = 同步间隔；无可用性测量）；AVL-02 自动故障切换 ➖（**自动选主明示不做**——两节点无仲裁必然脑裂 = 两个控制面同时签令牌；温备 + 人工触发切换脚本是真，"会话不中断"未验）；AVL-03 升级期影响 ➖（升级前校验/加密备份/灰度真，回滚是脚本；**无运维模式**）。 **OPS（4）**：OPS-02 终端自助诊断 + 一键日志 ✅ 真；OPS-01 B/S 免客户端 ➖（七层 Web 代理真，`web-e2e.sh` 九条断言；**跨浏览器未验**，应用间隔离靠不住见 ch8）；OPS-03 ➖（灰度/巡检/备份真，**SNMP 明示不做**——网关指标 + 业务告警 + syslog/SIEM 外送已覆盖可观测性主诉求；将来确有 NMS 生态需求再评估只读暴露 `gateway_metrics`，而不是补一套 MIB）；**OPS-04 门户品牌/文案定制 ❌ 不做**（无主题包上传下载、无 i18n、无帮助向导；与 ARCHITECTURE wave9 收口表判 FR-PORTAL-01~06 ❌ 同源，此前 SCOPE 侧未转述）。 **OBS（3）**：OBS-01 实时监控 ✅、OBS-02 日志检索/导出/外送 ✅ 两条真；OBS-03 ➖（外送真，SNMP 同上不做）。 **CMP（5）**：CMP-01 桌面端 ➖（Windows/macOS/Linux 三平台真，macOS 已端到端实测、Windows ARM64 阶段 A/B 过而 x64 未实机；**麒麟/统信未验**）；CMP-02 移动端 ➖（安卓壳 + gomobile 绑定真；**iOS 只有 `PacketTunnelProvider`，UI 壳未完成**；两端均未上架、未实机验证）；CMP-04 认证源兼容 ➖（本地/LDAP/AD/OIDC 真但未与真实目录实机互通，**RADIUS 明拒**，见 ch7）；**CMP-03 浏览器兼容矩阵 ❌**（无 `browserslist`、`vite.config.ts` 无 build target 降级、`tsconfig` target ESNext、无兼容测试、国产浏览器零提及；`webauthnSupported()` 那种运行期特性探测解决的是"单个 API 缺失时不白屏"，不构成一份验证过的浏览器清单）；**CMP-05 信创整机适配 ❌**（控制面只有 SQLite 一条路，store 层全部 `*_sqlite.go` 成对，无达梦/人大金仓适配层；国产 CPU 与麒麟/统信整机均未实测）。 **REG（3）**：REG-03 审计留存可配 + 防篡改 + 外置归档 ✅ 真（HMAC 链 + 留存轮转 + SIEM 外送）；REG-01 等保 2.0 ➖（身份鉴别/访问控制/安全审计/入侵防范四项各有对应实现，**未做等保测评**）；REG-02 数安法/个保法 ➖（留存可配、字段最小化，**未做合规评估**）。 ★以上"未实现/部分"逐条都是**边界**不是待办：没有规格表就别说并发达标，没有渗透测试就别说抗 OWASP Top 10，没有装过麒麟就别说信创适配——这正是这一整行原来那个 ✅ 主动关掉的检测。 |
| 21 | 集成、开放能力与数据模型 | ➖ | **这里原来是一个 ✅ 加一句「认证源 / SSO / 开放平台 API/SDK / 日志外送」——一个对勾盖住整章，而它与本文件 `ch8` 行、`ARCHITECTURE.md` 第七节自相矛盾。这正是 FR-INT-07/08/12/14/15/16 连续七波审计没被报出来的原因：对勾主动关掉了后续检测。逐节拆开如下。** **★小节编号已按 PRD 实际章节修正（wave9）**：此前这里写的 21.1/21.2/21.4/21.5/21.6 与 PRD 对不上——PRD 的实际编号是 21.1 模块定位 / 21.2 认证源 / 21.3 SSO / **21.4 安全设备联动** / 21.5 开放平台（含 API 与 SDK）/ 21.6 日志外送 / 21.7 数据模型 / 21.8 验收，于是 **21.4 整节没有对应行，FR-INT-09~13 落在了任何一行的账外**。逐节如下。 **21.2 认证源 ✅ 真**（LDAP/AD + OIDC，见 ch7；**RADIUS（FR-INT-03，P0）明拒**——保存即报错，无实现）。**21.6 日志外送 ✅ 真**（见 ch16）。**21.4 安全设备联动 ❌ 不做**：FR-INT-09/10（深信服 EDR/AC）与 ch13 同一条理由（私有协议）；**FR-INT-11 联动 VDI（P2）不做**——需要 VDI 平台的会话/策略接口与实机环境，写得出代码但验不了；FR-INT-12/13（第三方设备归一化上报与处置闭环）= 记档延后（M），承接侧已就绪、缺入口与第三方身份，见 ch13 行。**21.3 SSO ❌ 不做**：白帝可作为 OIDC **依赖方**（用企业 IdP 登录白帝），但**不做**「登录白帝后免认证代填进业务系统」那一半——那需要为每个后端维护凭据保管与表单/协议适配。**21.7 数据模型 ➖**：库结构就是模型，无独立交付物。**21.5 开放平台 ❌ 不做**，且缺的是三件而不是一件：①**机器身份凭据**（没有服务账号 / AK-SK / 长效受限令牌，集成方只能存一个人类管理员的口令并每 8h 重登）②**配额限流**（控制面无 per-token 配额；前置 nginx 有按源 IP 的粗粒度上界，见 ch20）③**任何形式的接口说明**。**21.5 的 SDK 那半 ➖ 一半真**：`gateway/mobile/baidimobile/`（gomobile 绑定，iOS `.xcframework` / 安卓 `.aar`）是货真价实的移动端接入 SDK（FR-INT-15）；**PC 端 SDK 没有**。**FR-INTRO-14 开放 API 联动 / FR-SEC-3RD-01~03 第三方风险摄入 = 记档延后（M）**：承接侧全部就绪（`risk.Evaluate` → posture 判定 → `PostureUsersByDisposal` → 网关 `DenyUsers`／撤窗断隧道），缺的只是入口与一套第三方调用方身份；本环境无真第三方设备可实测，硬做会重蹈「配置齐全但没人验过」 |
| 22 | 需求优先级与路线图 | ✅ | 白帝自有路线图（后续补 `docs/charter`） |

## Console IA（6 中心 + 系统管理，40 页全保留）

> ⚠️ 下面是**规划 IA**（自烛龙沿袭的目标形态），不是现状。实际落地的导航见 `console/src/nav.ts`：4 组 20 页，页名与此处的 `MonitorDashboard/IdentityUsers` 等规划名不一一对应。以代码为准。

```
概览     安全监控大屏 · 运行总览
身份中心  用户 · 终端设备 · 组织树 · 动态组 | 认证方式&MFA · IdP 联邦 | 认证策略 · 口令策略 · 账号安全 · 认证豁免 · 终端合规基线
资源中心  统一资源对象 · 资源审批 · 应用门户编排 | 地址/服务/时间对象
策略中心  统一策略 · 策略仿真器 · 登录流仿真 | 主动防御 · 关联规则 · IP 信誉名单
网关中心  网关清单 · SSL 接入点 · Mesh 中继/连接器 · IPSec 站点编排
审计中心  统一事件 · 审计链（HMAC-SM3）
系统管理  证书与密钥 · License · 管理员权限 | 日志外发 · 告警通知 · 时间&NTP · HA · 备份恢复 · 运营报表 | 系统诊断
```

无 UEM 中心、无安全中心管理页——白帝 IA 与"去 UEM / 去安全中心管理"边界天然一致。
