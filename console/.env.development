# 公共基础路径
BASE_PATH=/
# 路由模式：history | hash
HISTORY_TYPE=history
# 接口请求地址前缀
API_BASE_PATH=/
# token 存储键名
TOKEN_NAME=token
# 请求超时（毫秒）
TIME_OUT=60000
# 业务页面数据源：true=本地 Mock（默认，无后端可跑）；false=真实后端接口
USE_MOCK=false
