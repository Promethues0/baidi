# 白帝 · 零信任访问控制系统（ZTNA/SDP）

独立于烛龙的全栈自研 ZTNA（从烛龙 PRD 分叉做减法，取舍见 docs/SCOPE.md：UEM 整章不做）：SPA 单包授权服务隐身 + 国密 TLCP/TLS 隧道 + utun 真流量接管 + 身份/策略/审计闭环。控制台 21 页全部真实组件（由 check-dead-ui 规则四守住：nav.ts 每个 done 叶子必须在 router.ts BUILT 里）、数据面真链路实测、桌面 Tauri 客户端带 utun 数据面。在线演示 https://101.43.125.131/（admin/baidi@123）。研究/演示用途，未经安全审计。

## 交流与协作约定

- 全程中文（对话/注释/文档/commit）。独立 git 仓库。
- **主题是 Arco 原生 ArcoBlue #165DFF**，自定义变量一律 `--bd-*` 前缀（console/src/styles/tokens.css），明确不覆盖 Arco --primary——与烛龙黏土橙的做法完全相反，**不要把烛龙配色规范带进来**。

## 常用命令

```bash
cd console && npm run dev            # :5193（或 preview_start baidi-console），/api→127.0.0.1:8090
cd control && go run ./cmd/baidi-control   # :8090，SQLite 首启自动建表+播种
cd gateway && ./demo.sh              # 数据面最小闭环：暗→敲门→隧道→后端→TTL重暗（前置 control 已跑）
cd gateway && ./e2e.sh               # 全链路自检（自带起栈）：登录→剖面→敲门→钉扎→多资源路由→越权拒绝
cd gateway && ./ipsec-e2e.sh         # IPSec 全链路自检（无 root）：真 IKEv2 协商→真 ESP 加密→跨隧道业务流量→反例
cd gateway && ./web-e2e.sh           # 七层 Web 代理自检（无 root）：取票→换 Cookie→反代真后端→跨应用拒→撤权即断→XFF 剥离→票据重放拒→Cookie 不外泄/Host 不冒充→票据当 Bearer 拒
cd gateway && go run ./cmd/baidi-ipsec -h  # 站点组网守护进程（IKEv2+ESP，需 CAP_NET_ADMIN）
cd gateway && go run ./cmd/baidi-gateway -gm   # 国密 TLCP 隧道网关
cd clients/desktop && npm run dev    # :5294
cd clients/desktop && npm test       # 桌面端纯函数单测（接入态解析等，vitest）
cd clients/desktop && ./src-tauri/build-sidecars.sh && npm run tauri:build   # 打包前必先 build sidecar
cd clients/desktop && ./src-tauri/build-sidecars.sh --target aarch64-pc-windows-msvc   # 交叉出别的架构（CI 的 Windows ARM64 腿走这条）
cd clients/mobile && npm run dev     # :5295
cd clients/mobile && npm test        # 移动端纯函数单测（隧道状态监视判定，node --test）
cd control && go run ./cmd/baidi-standby -h   # 控制面温备节点（拉加密备份/校验/落盘，不提供服务）
cd control && go run ./cmd/baidi-license -h   # License 发行 CLI（发行方离线用；私钥绝不进控制面机器）
cd control && go run ./cmd/baidi-upgrade -h   # 升级包签名 CLI（发布方离线用；同上，私钥不进控制面）
cd deploy && cp config.env.example config.env && ./deploy.sh   # 一键部署
BAIDI_STANDBY_PASSPHRASE=… deploy/promote-standby.sh --dry-run  # 提升备机为主机（干跑：只校验+打印清单）
```

## 端口表

| 服务 | 端口 |
|---|---|
| baidi-control 管理 API | 8090（BAIDI_ADDR） |
| console dev / desktop dev / mobile dev | 5193 / 5294 / 5295 |
| baidi-gateway SPA 敲门 / TLS·TLCP 隧道 | 18201/udp / 18443/tcp |
| baidi-gateway 七层 Web 代理（`-web`，默认关） | 18444/tcp（约定值，**不受 SPA 隐身保护**） |
| baidi-ipsec IKE / NAT-T（ESP 走 UDP 封装） | 500/udp / 4500/udp（可 `-ike-port` 改） |
| baidi-knock-agent（dev 敲门代理，/knock 反代目标） | 8091 |
| 部署 nginx HTTPS | 443 独占机；与烛龙共存默认 **9443** |

## 架构地图

- `console/` — 单 SPA：管理台（监控中心/业务管理/安全防护/系统，21 页全部真实组件、无 ComingSoon 叶子）+ 门户 /portal/* + 大屏 /screen + 诊断 /diag；路由生成式：nav.ts 定义 IA → router.ts BUILT 映射
- `console/src/lib/api.ts` — 唯一 HTTP 封装：BASE=/api/v1，token 存 localStorage(baidi_token)
- `control/` — Go 控制面（**stdlib mux + Go 1.22 方法路由，无 gin**；modernc SQLite 免 CGO；自实现 JWT）；store 层 = 领域文件 + 同名 _sqlite.go 成对；两个二进制（baidi-control 管理 API / **baidi-standby 温备节点**，后者不开任何监听）
- `docs/ARCHITECTURE.md` — **架构与技术方案解析**（含接入时序图、信任模型、五道门、代码地图、真伪清单）。改动数据面/信任链前先读它
- `gateway/internal/webproxy/` — **七层 Web 代理**（B/S 免客户端）：票据校验 / 会话 Cookie / 逐请求鉴权 / XFF 剥离 / 反代
- `gateway/` — Go 数据面：9 个二进制（baidi-gateway / baidi-knock sidecar / baidi-knock-agent / baidi-tun utun 数据面(需root) / baidi-gmca SM2 签发 / baidi-tlcp-probe / baidi-e2e 全链路自检 / **baidi-ipsec 站点组网守护进程** / baidi-ipsec-e2e）；`internal/ipsec/` 自研 IKEv2+ESP（ike/ 控制面 · esp/ 数据面 · site/ 编排，依赖单向：ipsec←ike←esp←site）；firewall/ 内核态隐身脚本（pf/nft）
- `gateway/mobile/baidimobile/` — gomobile 绑定（iOS .xcframework / 安卓 .aar）
- `clients/desktop/` — Tauri 2 + Vue3，4 视图，osascript 提权拉起 root baidi-tun，托盘常驻
- `docs/` — SCOPE.md（对烛龙 PRD 逐章取舍）、design/00-ia-and-interaction.md（P1-P10 交互范式）

## 关键约定

- 鉴权：JWT Role ∈ admin|user|gateway；写操作 handler 内 requireAdmin()，数据面拉策略 requireGateway()。
- **管理员分级分权 / 三权分立（真执行方）**：角色落 `admin_roles("key",name,power,builtin,scope_json)`，归属落 `users.admin_role`（补列 + **一次性**回填既有 admin→root，`admin.role.backfill.v1`）。判定点是 `api.requirePerm(w,r,perm)`——读的是 `scope_json` 里的权限键（`system`/`security`/`audit`/`admins`/`*`），**不是 power 也不是页面文案**。审计管理员只读审计（`/api/v1/audit*`）、安全管理员管认证源/策略/资源/用户组织/审批但读不到审计、系统管理员管网关证书/组网/对象库/`/diag`。**角色现算不进令牌**（降权要立刻算数），读不到角色一律 fail-closed 403 且落审计——**读端点同样现算**：`requireAdmin` 与 `requirePerm` 共用 `currentAdminRole`，撤销/禁用后旧令牌立刻读不到目录与在线会话（此前读面停在 8h 令牌快照上）。**改管理员的口令与状态需 `admins` 权**（`api.guardAdminTarget`，判据是目标 `users.role='admin'`）：只查 `security` 的话，安全管理员重置一次超管口令就能登进去拿全权，或把审计/系统管理员禁用掉——防自锁只管「最后一名超管」，管不到另外两权。**防自锁**：最后一名可登录超管不可降权/撤销/禁用（事务内计数，409；已禁用的 root 不计入）；自定义角色拒收 `*` 与 `admins`（否则等于自造一个不叫 root 的超管，防自锁计数绕过去）。`POST /api/v1/users` 已收口成只建普通用户，建管理员唯一入口 `POST /api/v1/admins`。**集群 = 温备**：System 页与 `/diag checkCluster` 同口径（`api.clusterView` 一处实现），见下方温备条目。
- 配置全走 `BAIDI_*` 环境变量（BAIDI_ADDR/BAIDI_DB/BAIDI_JWT_SECRET/BAIDI_GW_SPA…）。
- **授信终端（设备准入）**：`trusted_devices(account, fingerprint, status ∈ pending|trusted|revoked)` 是设备台账，与 `posture_reports` 按 (账号,指纹) 一一对应（单账号上限 `store.MaxDevicesPerAccount`=20 只有一份定义，两表同删）。首次 posture 上报自动登记（`auto` 直接 trusted / `approval` 入 pending 并生成一条**复用既有 approvals 表**的审批单）。准入执行方是 `api.deviceAdmissionGate` → `handleKnockToken`：**strict** 拒发非授信设备的敲门令牌，**observe**（默认）放行并按 (账号,指纹) 节流 5 分钟记审计；**`revoked` 两种模式下都拒**（否则默认配置下「吊销」是空动作）。指纹由客户端自报，唯一来源是桌面端 `collectPosture().device` → `store.device` → posture 上报 / 登录 deviceId / `baidi-tun -device`。**会话撤销只能按账号**（数据面 revoked 通道无设备维度），影响面文案由 `api.deviceRevokeBlastRadius` 统一下发。设置页 `PUT /api/v1/devices/settings`（PermSecurity）。**资产分类与标签（FR-EP-06~09）**：补两列 `asset_class`(enterprise|personal|managed，回填 enterprise) 与 `tags`（回填 `[]`，一次性标记 `device.asset.backfill.v1`）。分类是**准入判据**，执行方与判定粒度都在 `deviceAdmissionGate`——设置项 `personalPolicy` ∈ `inherit`(默认，行为不变)/`strict`(个人资产恒按严格判)/`deny`(个人资产一律拒，含已授信)，`managed` 按企业资产处理（`store.IsPersonalAsset` 一处判据）。**刻意不并入 degrade**：那条通道是账号维度的（`PostureUsersByDisposal`→网关 `DenyUsers`），一个人同时有企业机与个人机时会把企业机一起误伤，而准入闸天然是 (账号,指纹) 粒度（`TestPersonalDenyDoesNotAffectEnterpriseDevice` 钉住）。拒绝原因必须点名资产分类（泛泛的"终端未授信"会把人支去找管理员批准，而批了也进不来）。**`tags` 没有执行方就如实说没有**（只用于筛选/导出/盘点，页面当面标注「仅台账属性」、分类那栏标「真实判据」）。真正的「按设备区分能访问哪些资源」需要把指纹贯穿数据面，**本波不做**，边界见 docs/ARCHITECTURE.md 第七节。
- **接入策略（FR-POLICY-29/30，wave8 行动 13-①）**：`store.AccessPolicy` 落 `settings`（键 `policy.access.v1`），执行点唯一——`api.accessSessionGate` → `handleKnockToken`（**第五道闸，排在设备准入之后**：那道问"这台设备允不允许接入"，这道问"此刻的名额与活跃度"）。**同时在线设备上限**：0~1000，**0=禁止接入**（PRD 原文），故必须另有 `DeviceLimitEnabled` 开关——用 `MaxDevices==0` 表示"不限"的话，存量库零值 = 升级重启那刻全员被挡在门外；`ParseAccessPolicy` 对坏数据一律回落成"两条都关"（回落方向恒定为不生效）。名额**先到先得**（排序键 `(first_seen, 指纹)`，带指纹是因为秒级 first_seen 会让 `sort.Slice` 抖动 → 两台轮流被踢），**新终端永远排在已在线的后面**（否则同秒接入时凭字典序顶掉正在用的那台），**被拒的新终端要回滚记账**（记账在判定之前，留着会在下一轮排名里参与竞争）。**平台取自 posture 上报不从敲门请求取**（让被判定方自报判据 = 改个字符串就换名额池），取不到按 PC 计。**接入超时注销**：判据是**业务流量**不是敲门保活（客户端不退出就每 15s 敲一次，拿保活当活跃 = 规则永不触发）——信号源 `spa.Allowlist.Touch(ip)`（proxy 在**两道放行复核之后**调，否则往隧道口打个包就能替别人续命）→ 心跳 `sessions[].lastActive`（**三态指针**：nil=旧网关不报=不可判定 / `&0`=报了但从未有业务连接 / `&ts`；写成 `int64+omitempty` 会把前两者压成同形，而处置正好相反，把 nil 当 0 = 一台没升级的网关每 15s 给所有会话盖 1970 年的戳）→ 控制面按 **(账号, 源IP)** 落 `device_sessions`（网关按源 IP 记会话、不知道指纹；同 NAT 出口两台互相顶替，fail-open）、**只往前推不倒退**。**注销状态是粘的**（关掉规则不该让它自己活过来），解除只有一次完整认证，且恢复是**删行**不是改 state（只改 state 的话空闲时长会从上一段会话的 `first_seen` 起算 → 登录成功后立刻又被踢），**按账号恢复不按设备**（TOTP/passkey 第二回合拿不到指纹，按设备则开了 2FA 的部署永远解不开）。同批**摘除**「用户策略 · 继承编辑器」8 项 + `policy_overrides` 读写 + `OrgNode.HasCustom` + `GET/PUT /policies/{node}` + `GET /policies`（表保留）：那 8 项全仓零消费方而保存 toast 谎称「已下发至代理网关」。**按组织分级的策略继承（FR-POLICY-02~05）与「无键鼠操作」超时明确不做**，策略页当面列出。
- **安全基线的适用范围是真判据（wave8 行动 13-④）**：`BaselinePolicy` 摘掉无人读的 `Type`、把自由文本 `Scope` 换成 `ScopeOrgs`/`ScopeGroups`（补两列 + 回填 `'[]'`，`backfillBaselineScope`）。过滤点 `api.baselinesInScope` 在 `risk.Evaluate` **之前**——Evaluate 是纯函数不碰 IO，把取数塞进去就再也测不动了。**两栏都空 = 对全体生效**（与自由文本时代的实际行为逐字一致，故回填后判定不变）；组织含子树，判定收敛到 `store.SubjectIndex.Covers` 一处（资源授权 / 认证策略 / 基线三处共用，`authpolicy.covers` 同批改成调它）。**展开索引读不到时保留全部基线**（fail-closed：基线是闸门，读失败不该让全体瞬间"合规"——与「认证源不可用绝不动手」方向相反是有意的）；引用不存在的组织/用户组保存即拒（共用 `validateSubjectRefs`），不拦的话那条基线对谁都不生效而页面显示「已启用 · 阻断」。
- **终端 posture / 风险引擎**：`POST /api/v1/posture` 上报（登录用户，platform 枚举 Windows|macOS|Linux，每账号 ≤20 台设备）→ `internal/risk.Evaluate` 按安全中心基线（`baseline_policies` 表，安全中心页可编辑）评估 → 最新判定 block 则 knock-token 403 + 经 `gateways/policy` revoked 捎带撤窗断隧道；判定权全在控制面。缺报默认放行（observe），`BAIDI_POSTURE_ENFORCE=strict` 缺报/过期（10min）也拒。基线检查 key 与桌面采集器对齐：disk_encrypted/sys_integrity/firewall_on/os_version/edr_online/client_version。
- **风险四档都有执行方（PRD 1.5「降权优先于全断」）**：严厉度 `allow < gray < degrade < block`，排序表只在 `store.DisposalRank` 定义一份。**gray** = 访问权不变 + 每轮策略下发记一条 observing 审计（`api.auditGrayObserved`，按 5min 节流，否则 30s 轮询会把审计冲成噪声）；**degrade** = **降权不断连**，只把高敏资源摘掉——判据是 `resources.sensitivity`(low|normal|high，补列 + 两步回填，`finance` 类应用关联的资源抬成 high，第二步带一次性标记防"改了重启就变回去")，网关侧经新增的 `resource.Resource.DenyUsers` 否决、剖面侧经 `accessibleFor` 剔除，**两处同构有测试同时断言**；**block** 行为完全未改。**`DenyUsers` 必须先于一切允许来源判定**——JIT 授予是并进 `AllowUsers` 的，先判允许等于一张审批单就能绕过降权。网关仍不做推导（不知道"高敏"是什么），只执行控制面算好的名单。降权必须让用户知道：剖面 `warnings` 第一条写明原因与影响面，磁贴带 `degraded` 区分"没授权"与"被降权"（两者下一步动作不同）。用户状态页分桶已与四档统一（`block/degrade/gray` + `locked/disabled`，原 `risk-high/risk-low/idle` 已删）。
- **采集三态（`ok` / `unknown`）**：采集器（`clients/desktop/src-tauri/src/posture.rs`，Windows/macOS/Linux 三平台）对**探不到**的项报 `unknown=true` 而非 `ok=false`——Linux 非 root 读不到防火墙、Windows 非管理员读不到 BitLocker 都是常态，塌缩成 false 会把**真实合规的终端误拒**，塌缩成 true 是误放行，两种错法在页面上都看不出来。`risk.Evaluate` 按 `Options.StrictUnknown` 处理：observe（默认）不计分不抬处置、只单列进 `Verdict.Unknowns` 展示，strict 与「缺报即拒」同口径视为不合规。**判 unknown 必须先于判 ok**（unknown 时 ok 恒 false，是给旧控制面的 fail-closed 兜底），三处渲染同此顺序。采集逻辑抽在 `Env` trait 后面，三平台解析在任意主机上都编译+单测——只活在 `#[cfg]` 里的分支在 mac 上连语法都验不到。
- **身份密钥（CA 迁移已收口）**：control 持**三把 Ed25519 私钥**按用途分签——`BAIDI_JWT_KEY` 签会话令牌与 MFA 票据，`BAIDI_JWT_KNOCK_KEY` 只签敲门令牌，`BAIDI_JWT_WEB_KEY` 只签七层 Web 代理票据（首启自动生成 0600，公钥写同名 `.pub`）。**网关的 SPA 监听只装 knock 公钥、L7 监听只装 web 公钥**（`-jwt-pubkey`，逗号分隔可多把供轮换）：会话令牌的 kid 在网关侧查不到，**从密码学上就敲不开门**，`spa.checkKnock` 的 use 语义闸退化为纵深而非唯一防线。**公钥用部署期文件分发，刻意不做 JWKS 端点**——在线端点若自身即信任根会构成循环论证。
- **网关机器身份 = mTLS 客户端证书（唯一路径）**：control 内部 CA（标准 X.509/P-256，`BAIDI_PKI_DIR`）签发，`POST /api/v1/pki/gateway-certs` 取证、`…/{fingerprint}/revoke` 吊销（指纹白名单是即刻吊销的执行点）。网关配 `-mtls-cert/-mtls-key/-mtls-ca` 经 `BAIDI_MTLS_ADDR` 独立端口调控制面；**配了 `-control` 却没配证书直接拒绝启动**。`/api/v1/gateways/*` 只挂 mTLS 监听。SM2 国密 CA 继续只管 TLCP 隧道，两套 PKI 互不污染。
- **`gateway/internal/auth` 刻意没有 Sign 函数**：数据面在代码层不具备签发能力（阶段 4 删除）。要加签发就是在给被保护方发钥匙，别加。
- **收口默认值与逃生舱**：`BAIDI_ACCEPT_HS256`、`BAIDI_GW_ACCEPT_HS256`、`BAIDI_GW_PLAINTEXT_COMPAT` **均已默认 false**。三者置 1 是过渡逃生舱（升级瞬间尚有未过期的 8h 会话时临时用），存量过期后必须关回。`BAIDI_JWT_SECRET` 收口后已不承担跨进程职责，仅逃生舱路径用得到。
- **严格敲门（strict knock，默认开）**：网关只接受 control `/knock-token` 签发的短时效一次性令牌（`use=knock` + jti + ≤`-knock-max-ttl`，见 `spa.checkKnock`）。长效会话令牌**不能**再直接敲门——那会绕过强制下线/账号禁用/终端合规三道闸。因此**所有敲门客户端必须能访问 control**（`baidi-knock`/`baidi-tun`/`tlcp-probe` 的 `-control` 已必填，knock-agent 有默认值）。逃生舱 `BAIDI_GW_KNOCK_STRICT=0` 仅限过渡。副作用：control 不可达超过网关 `-ttl`(30s) 时窗口自然关闭（fail-closed，零信任下是正确姿态，不再回退长效令牌硬撑）。
- **客户端接入剖面（终端能连通的前提）**：`GET /api/v1/client/profile`（登录用户）一次下发**网关落点 + 该接管哪些网段 + "host:port→资源id"映射 + 隧道证书指纹**。终端**不推导策略、也不自己猜网段**——只有控制面同时知道网关在哪、业务在哪、当前用户有权访问什么。剖面里同时登记 VIP 与业务真实地址（两种访问姿态收敛到同一 `CONNECT <id>`），VIP 按资源 id 字典序**确定性分配**（不稳定会让用户书签/SSH 配置失效）。**剖面不是授权凭据**，只是路由提示：权威授权闸始终在网关侧 `resource.Authorize`，泄露剖面拿不到任何访问权。可访问性判定必须与 `handleGatewayPolicy` 同构（静态 ACL ∪ 组织/用户组展开 ∪ 有效 JIT 授予），否则会出现「审批批了却连不上」。
- **网关多活与客户端故障转移（FR-ARCH-03/04）**：剖面下发**有序**的落点清单 `gateways`（`api.profileGateways`），排序是**在线优先 → 网关 id 字典序**——刻意不按「心跳谁更新鲜」，那个序每 15s 翻一次，客户端每次拉剖面都换首选落点，表现为隧道莫名重连。**离线网关排末尾但仍下发**（控制面抖一下不该让客户端丢掉可用落点）。**单数 `gateway` 字段恒等于 `gateways[0]` 且必须保留**——存量终端只读它，改成只发列表就是升级那一刻全员掉线。**指纹逐网关**（`TunnelPin` 挂在落点上）：共用一份会让故障转移卡在钉扎那步，症状是「切过去就连不上」。在线判定只有一处实现 `api.gatewayFresh`（网关页 / 剖面 / 诊断 / Web 入口共用，此前剖面自己写了个 90s 而网关页是 120s）。数据面侧 `dataplane.picker`：**每轮保活敲全部落点**（没敲过的落点在切换那一刻只会拨号超时）、**逐落点各取一张敲门令牌**（jti 去重是每台网关各自做的，同一封包发两处等于让截获者能给自己开窗）、切换后粘住新落点、并打一行结构固定的日志 `endpoint=i/n id= addr= reason=` 供桌面接入页解析展示（改键名要同步改 `tunnel.ts` 的正则）。**就近选择与负载均衡刻意不做**（无地理/时延数据；心跳指标无调度语义，按 CPU 选最闲会让所有新连接一起涌向同一台）。
- **控制面温备（PRD 15.5，`internal/standby`）**：**温备不是双活**——SQLite 单写者，两个实例同写一个库会在写冲突时**静默丢配置**，比没有 HA 更糟。备机 `baidi-standby` 周期拉加密备份（**复用 `upgrade.CreateBackup`，不另造格式**）→ 校验（解密 + 必须含 `baidi.db`）→ 原子落盘 → 回报；**校验不过绝不覆盖本地那份**，且它**不开任何监听**（"备机不是第二个控制面"由进程里没有 http.Server 保证）。主机侧 `GET/POST /api/v1/standby/{backup,status}` **只挂 mTLS**、CN 须 `standby-` 前缀（照 `ipsec-` 分权），明文口一律 403；签发接口**拒收 `standby-` 前缀**（CN 前缀是这道分权的唯一判据，能签就能绕），备机证书只在主机上离线签。反向备机证书也调不到 `/api/v1/gateways/*`（否则剖面会把它当落点下发给终端）。**别再说「管理员令牌拉不走备份」**：`POST /api/v1/upgrade/backup` 产出的是逐字节同源的同一份归档（同一个 `backupSources`），口令还由调用者自定；它现在收成 **`PermSystem ∩ PermAdmins`**（实际只有 root）——解开备份就能拿 `BAIDI_JWT_KEY` 自签一张 Name=某 root 的令牌拿全权，「能拿走全部信任材料」必须等价于「能造任意管理员」。**新鲜度只看「备机回报校验通过」，不看「它来拉过」**（`standby_nodes` 两列分记；拿拉取当判据会把"准时来拉、每次校验都失败"显示成健康），落库一律服务端时间。逐节点阈值 `max(全局 15min, 3×备机自报间隔)` 且**封顶 6h**（间隔是被判定方自报的）；「从未成功同步」的 `lagSeconds` 是 **-1 不可判定而非 0**。**四态**由 `api.clusterView` **一处**产出（System 页与 `/diag checkCluster` 同源）：未配置 skip / **已签发 `standby-*` 证书却零台账 = warn**（台账行只在备机真连上 mTLS 口时才建，「证书签好了、备机在跑、mTLS 口只听回环」此前与「根本没配」完全同形，而它正是切换那天没有备份的形态）/ 新鲜 pass / 落后 warn。切换是真脚本 `deploy/promote-standby.sh`（校验→解包→停服→快照→覆盖→起服→自检），**干跑逻辑有 Go 用例真的执行它**；**RPO = 同步间隔、切换需人工触发、不做自动选主**（两节点无仲裁必然脑裂 = 两个控制面同时签令牌）。**备份里的库是 `store.SnapshotTo`（`VACUUM INTO`）出来的一致性快照**，绝不能直接读活库文件——WAL 模式下提交只落 `-wal`，整读主库拿到的是「上次 checkpoint 为止」的内容，而备份解得开、含 `baidi.db`、备机校验通过、页面显示「同步新鲜」，真实 RPO 无上界，只在切换那天暴露。配置备份的材料清单顺带补齐：审计链 HMAC 密钥改问 `store.AuditKeyPath()`（原先读默认为空的环境变量，标准部署备份里根本没有它 → 恢复后全链校验永久失败）、`BAIDI_JWT_WEB_KEY` 补进去（原先漏 → 恢复后 B/S 全挂而隧道正常）。
- **隧道服务端认证 = 证书指纹钉扎**：网关隧道证书是启动期自签的，没有公共 CA 可依赖。网关算出自己证书的 SHA-256 指纹随 mTLS 注册心跳上报 → control 经剖面转发 → 客户端 `VerifyPeerCertificate` 比对（`dataplane.tlsClientConfig`）。代码里的 `InsecureSkipVerify: true` 关掉的是**链**校验，安全性由钉扎承担——钉扎比链校验**更严**，只认那一张证书。指纹口径两侧必须一致：都对**叶子证书 DER 原文**取 SHA-256。国密 TLCP 走 CA 校验，两条路径信任材料不同，不可混用。
- **分离式 DNS（split-DNS）**：剖面 `dns` 段下发解析器 VIP + 分流域 + `FQDN→VIP`；客户端在 netstack 里跑隧道内解析器（`dataplane/dns.go`），并按域配系统解析器（macOS `/etc/resolver` / Linux `resolvectl` / Windows NRPT）。**不做递归转发**（未知域名 REFUSED，避免泄露内网查询 + 变成开放解析器）；**AAAA 命中名字回 NOERROR+0 而非 NXDOMAIN**（回 NXDOMAIN 会让客户端连 A 都不再查，且只在双栈机器上出现）；**UDP 只服务 DNS**，其余 UDP 回 ICMP 端口不可达而非黑洞（黑洞会让 QUIC 卡到超时才降级）。解析器 VIP 用网段基址 +53 并从资源分配区间**挖除**。系统解析器配置那一段**未实机验证**，退出清理覆盖 defer/信号/异常退出三路。
- **`baidi-tun -route` 支持逗号分隔多网段**：业务地址散落在多个内网段，只接管单一网段就会出现「隧道建起来了、点开应用却直连不走隧道」——这是本项目历史上最迷惑人的失败形态（无报错、UI 显示已接入）。网段清单由剖面下发，不该由用户手填。
- **认证源接入（LDAP/AD + OIDC 已真实现）**：配置落库 `auth_sources`，凭据走 `auth_source_secrets` 独立表加密（AAD 绑源 id，**只写不读**）。登录链路「先本地、后外部」——反过来等于把本地 admin 的认证权外包出去。**账号绑定以 `Subject`（OIDC `sub` / LDAP `entryDN`）为键而非用户名**：按用户名绑的话，AD 里新建一个 admin 就能冒充本地管理员且审计看起来完全正常；撞名加来源后缀、外部账号 `role` 恒 user、`pass_hash` 恒空（否则停用认证源后账号退回成"某个本地口令也能登录"）。外部账号建号时**必须落 `org_id`**（`ensureExternalOrgUnit`，缺失即当场建回外部目录组织）+ 一次性回填 `org.extuser.orgid.v1`：只写 `org_key` 的话 `SubjectIndex` 按 `org_id` JOIN 会把这批人整体排除，组织授权（fail-closed）与策略适用范围（**fail-open**）都覆盖不到他们。**认证源故障不能回「密码错误」**也不计入锁定。LDAP 客户端用 go-ldap（**刻意不自研**：与 IKEv2 相反，LDAP 的价值就是连别人的 AD，自研只增互通风险）；OIDC 用标准库。RADIUS/短信/证书三类未实现，保存时明确拒绝。**均未与真实 AD/IdP 实机互通验证**，边界见 docs/ARCHITECTURE.md 第七节。
- **组织与用户组（目录维度，非授权维度）**：`org_units`（邻接表 + 冗余物化路径 `path`，形如 `/root/dev/`）、`user_groups`（`kind=static` 显式成员 / `kind=role` 按 `users.roles` 派生只读）、`user_group_members`（**按 account 存**，account 是令牌主体，将来接授权才对得上）、`users.org_id`。REST 在 `/api/v1/orgs`、`/api/v1/groups`、`PUT /users/{id}/membership`，全 admin + 落审计；控制台在「业务管理→用户与角色」页内维护，不另开导航。冗余 `path` 的用途是**环检测**（把父设成后代时，该父的 path 必含 `/<自己 id>/`）与子树查询——改父必须级联重写子树 path。删除守卫：有子部门或有成员一律拒删（**不做级联置空**：静默失去归属 = 静默丢策略分组）。种子那 4 个部门由 `backfillOrgUnits` 建成真实行并回填既有用户，**调用点在 seed() 之后**（全新库跑 migrate 时 users 还是空表，放 migrate 里会静默空转），一次性标记落 `settings`（否则管理员删掉的部门会被下次启动复活）。
- **按组织/用户组授权（主体维度已接进判定）**：`resources.allow_groups / allow_orgs` 两列（补列 + 回填 `[]`，见 `backfillResourceSubjects`），组织**含子树**——授权给某组织即涵盖其全部后代组织的用户。**子树展开只有一处实现**：`store.SubjectIndex`（`store/subjects.go` 纯逻辑 + `subjects_sqlite.go` 取数，靠冗余 `path` 一次展平祖先链）。控制面两个判定点都从它取答案且必须同真同假——`handleGatewayPolicy → expandForGateway`（把组织/组展开成账号并进 `AllowUsers` 下发）与 `buildProfile → authorizeRes`（剖面可达性）。**网关侧 `resource.Resource` 与 `registry.Authorize` 一字未改**：数据面不知道组织树，也不做子树推导，判定权全在控制面。展开**每次现算不缓存**——把人移出组织下一轮轮询即失效。**空展开必须下发哨兵 `store.DenyAllSubject`**：成员为零的组织授权若原样下发，网关会因「两维皆空 = 不限」退化成对所有人开放，与控制面判定方向完全相反且两侧都不报错。
- **消息通道（`internal/notify`，PRD ch15.2）**：`smtp`（net/smtp + TLS，STARTTLS/implicit，匿名/PLAIN/LOGIN）与 `webhook`（POST JSON，非 2xx 即失败）是真实现；**`sms` 就是一次 webhook 调用**（载荷 `{mobiles,text}`），白帝不实现任何短信网关协议，UI 与 `smsNote` 都必须照实说，别写成"已支持某某云短信"。**StartTLS 失败绝不降级明文**（与 ldapsrc 同款纪律，对照测试断言服务端一条 AUTH/MAIL 都没收到）；明文传输 + 配置认证在构造期就拒。配置落 `notify_channels`，凭据走 `notify_channel_secrets` 独立表 AES-256-GCM、**AAD 绑 channel id、只写不读**。REST 在 `/api/v1/notify/channels*`，**归 `PermSystem` 一权**（给 security 的话，安全管理员能把告警改投到自己邮箱）。**真实消费方**：爆破锁定（`noteLoginFailure`）与终端判定**转入** block（`handlePostureReport`）各发一条——按"当前是 block"发会随上报频率刷爆邮箱，判据必须与审计那条转换判据同源。派发走 `notify.Dispatcher` **有界异步队列**（满则丢新保旧并计数，计数经 `droppedNotices` 下发）：消费方在主流程上，一台连不上的 SMTP 会变成比爆破更省事的拒绝服务面。发送成败**都落审计**（行为人 `system`，见 `auditBG`——异步动作记到某个管理员头上是最难自证的错记）。**`last_status/last_detail/last_event/last_at` 只由真正发出那一次写入**，`SaveNotifyChannel` 的 upsert 分支刻意不碰这四列，否则通道页会在邮件根本发不出去时长期显示绿色。无重试、无 App Push、webhook URL 不做出网限制（SSRF 面，接受的边界，见 docs/ARCHITECTURE.md 第七节）。
- **审计外送 Syslog/SIEM（`internal/forward`，PRD ch16 + ch21.6）**：`syslog` = RFC 5424 报文 + RFC 6587 帧（octet-counting / LF），**只走 TCP**，可选 TLS；`http` = 通用 JSON 批量出口。**刻意不做 UDP**（审计用 UDP 会静默丢包，两端都看不见），TLS **不给跳过证书校验的开关**（证书对不上填 `serverName`/`caCert`；有反例用例守着）。**外送必须带链的 `seq`/`mac`**——那是 SIEM 侧独立验真的唯一依据，也是本功能区别于"日志复制"的地方；三个出口（`/audit` 列表、CSV 导出、外送）共用**同一个 `store.AuditEntry`**（`forward.Record` 是它的类型别名），加字段改一处三处跟上。**可靠性**：审计落库的同一事务里给每个启用中的出口入队（`audit_forward_queue`），后台 pump 批量投递、**成功才出队**、失败整批留队退避（5s→15min 封顶，`forward.Backoff`）；入队失败只 `slog.Error` 不回滚（少一条外送远好过少一条审计）。**选独立队列表而不是在 `audit_log` 加 `forwarded` 列**：加列要配一次性回填把既有行标成已处理，漏了就会在开启外送那一刻把 180 天历史整段重发；队列表让"不重发历史"结构性成立。队列**有上界**（`BAIDI_AUDIT_FORWARD_QUEUE_MAX`，默认 20000/出口，无上界=把库写满进而让审计本身落不了库），溢出**丢新保旧**（留连续的最早一段，SIEM 侧 seq 仍连续）+ 丢弃计数落库 + 控制台红条 + 节流转一条 security 审计。配置落 `audit_forward_targets`，凭据走 `audit_forward_secrets`（AAD 绑 target id、只写不读；**syslog 出口没有凭据可设，设了直接 400**）。REST 在 `/api/v1/audit/forward*`，**归 `PermSystem`**（`PermAudit` 按设计只读，不持写端点）。投递循环 `StartAuditForwardLoop`（`BAIDI_AUDIT_FORWARD_INTERVAL`，默认 5s）是唯一执行方。**未与商用 SIEM 实机对接验证**，边界见 docs/ARCHITECTURE.md 第七节。
- **应用可改可删 + 直连书签（wave8 行动 14）**：`PUT/DELETE /api/v1/apps/{id}`（`PermSecurity`，与发布同权）。**路径 id 说了算、请求体 id 一律忽略**（否则「编辑 A」会改到 B 身上而 URL 与审计都记 A）；**UPDATE 必须查 RowsAffected**（SQLite 对不存在的 id 不报错 → 改一个已被删掉的应用静默成功）；改分类与建应用同款事务 + 字典校验。**下架不级联删受控资源**（可能被别的应用引用、也可能挂着 JIT 授予），回执与确认弹窗都要说清「访问控制仍按资源策略生效」——不说的话管理员以为下架顺手收权了；**重复下架回 404 不回 200**（否则审计里出现一件没发生的事）。摘掉 `apps.node`（管理员没有这个输入项，`CreateApp` 一律写死「华东出口」，唯一消费方是一列恒值表头；列保留，表格那栏换成「关联资源」）。**`mode=global` 正名为「直连书签」**：它不经网关、不进隧道路由、不做鉴权（`appAccessState` 直接给 `Accessible: true`），三处名字（向导/门户/移动端）统一 + 向导与编辑抽屉各挂告警 + 门户按钮文案与真实行为一一对应（此前对 global 提示「请用桌面客户端接入后访问」，而它压根不走隧道，等于把用户指去一条不存在的路）。**泛域名代理仍不做**。★`app_categories` 里那个 key=`global`（标签「全网资源」）是**分类**维度，与发布形态的 `global` 只是同名，别顺手一起改。
- **应用分类是真实字典（可自建可修改）**：落 `app_categories("key",label,sort,builtin,created_at)`，内置四类由 `backfillAppCategories` 建成 `builtin=1` 的真实行（一次性标记 `app.categories.backfill.v1`，**调用点在 seed() 之后**——它还要收养「库里 `apps.category` 出现过、却不在内置清单里」的历史自由文本值，全新库在 migrate 阶段 apps 还是空表）。**`catOrder`/`catLabels` 两个包级常量已删除**：分类能在页面上改，再留一份编译进二进制的清单就是第二个真相来源。`Apps()` 的分类栏由表构建，首项「全部应用」是**合成项、不入表**（真入表的话它的 count 只统计 `category='all'` 恒为 0，与"应用总数"撞语义），故 `all` 是保留 key。REST 在 `/api/v1/app-categories`（读=任意管理员现算角色，写=`PermSecurity`，与 `POST /apps` 同权）。**内置分类可改 label 与 sort、不可删**（`builtin` 列的真实消费方就是这道守卫）；**key 一律不可改**（`apps.category` 按值引用，没有外键）；**分类下有应用拒删 409 并报出数量**，不做级联置空。`CreateApp` 校验分类必须在字典里（`ErrUnknownAppCategory`→400）：字典外的 key 会让该应用在筛选条**任何一栏都不出现**，只有「全部应用」看得到，而接口照回 201。**`backfillResourceSensitivity` 里的 `apps.category='finance'` 是一次性历史语义迁移、不是分类字典的用法**（marker 守着只跑一次，迁完高敏的唯一判据是 `resources.sensitivity`）——别再按「分类=某某」派生任何安全判据，分类现在是管理员可改的自由数据。
- **七层 Web 代理（B/S 免客户端，PRD 8.3.3）**：`gateway/internal/webproxy`（`-web` 监听，**默认关**）+ `control/internal/api/webproxy.go`。链路是「门户登录 → 控制面按资源鉴权后签 `use=web` 短时效一次性票据（jti + **绑定资源 id** + 60s，用**第三把** Ed25519 密钥 `BAIDI_JWT_WEB_KEY` 签）→ 浏览器跳 `/__baidi/enter` → 网关只用 web 公钥验票 → 换网关**本机** HMAC 会话 Cookie → 逐请求重新鉴权 → 反代」。几条不许动的纪律：**`use` 语义闸三向**（敲门拒 web、L7 拒 knock、**控制面入站两种都拒**——`auth.Middleware` 是用途白名单，只有会话令牌能调 API；缺了这道，一张 60s 资源票就是该账号 60s 的全量 API 会话且能自我续签）且三条路径**各装一把密钥**，拿错票在对面连签名都验不过；**一次性有执行方**：网关侧 jti 去重缓存 `webproxy.Server.ticketUsed`（同 `spa.checkKnock`），票据另带 `gw`=目标网关 CN——去重缓存是每台网关自己的内存，不绑网关的话去重会被机器台数整除；**网关不得获得签发能力**（Cookie 只是本机会话状态，控制面从不验它，`internal/auth` 仍无 Sign）；**Cookie 绑 (账号,资源)** —— HttpOnly+Secure+SameSite=Lax+`Path=/app/<资源id>/`，且服务端复核绑定（浏览器 Path 规则挡不住手工构造的请求）；**每个请求都重查 `resource.Authorize`**（含 DenyUsers），强制下线/降权/JIT 到期一个轮询周期内生效，**不是只在建会话时判一次**；**进站 XFF/X-Real-IP/Forwarded/X-Baidi-* 一律先剥再按真实来源重写**——真实来源由 `-web-trusted-proxies`（显式可信代理网段）决定，不配的话推荐部署（回环 + 前置 nginx）下后端看到的客户端 IP 恒为 127.0.0.1、X-Forwarded-Proto 恒 http；`X-Forwarded-Host` 只在可信来源或 `-web-external-host` 下发，**绝不拿客户端可控的 Host 头当真实值**（Host header injection）。**101 升级（WebSocket）后没有「下一个请求」**：那类连接登记进 `upgradeTracker`，周期复查同判据 + `Server.KillUser` 参与强制下线（与 `proxy.KillUser` 成对），寿命不超过会话 Cookie。**应用间隔离靠不住**：所有应用共用网关这一个浏览器源，A 页面里可用 B 自己的 Cookie 发同源请求，Referer 判定只是纵深，真隔离只有给每个应用配独立域名（`webEntry`）。后端的 `__Host-` Cookie 会被改名（`bdhostpfx-`）再改回，网关自己的 `baidi_web` 不转发给后端。三道账号闸（封禁/账号状态/终端合规）与敲门**共用 `api.entryGates`**；设备准入那道对浏览器不生效（浏览器无指纹），`BAIDI_POSTURE_ENFORCE=strict` 与 B/S 接入互斥。资源补两列 `web_scheme`（拨号协议，回填按端口推，归一只在 `store.NormalizeWebScheme` 一处）与 `web_entry`（对外入口覆盖，空即"用网关落点"故不回填）。**L7 端口不受 SPA 隐身保护**（发布向导当面告警 + 网关启动告警）。**HTML 正文绝对链接不改写、内网 HTTPS 后端不校验证书、DLP/水印不做**——边界见 docs/ARCHITECTURE.md 第七节，别在 UI 上加回那批假开关。证据是 `gateway/web-e2e.sh` 九条断言。**入口主机名取数优先级**（`api.webEntryBase`）：资源 `webEntry` → `BAIDI_WEB_ENTRY_BASE` → **网关页登记的对外接入地址**（与剖面共用 `gatewayAccessMap`，**只登记一栏时**才用）→ 网关自报监听 host；后两档推出回环/通配/空一律**不就绪**（`webHostUnroutable`，门户置灰 note 与取票 503 同一句话，绝不签一张指向 127.0.0.1 的票——参考部署 `-web 127.0.0.1:18444` 此前正是这样零报错地不生效；本机 e2e 靠显式 `BAIDI_WEB_ENTRY_BASE` 跑，登记接口拒收回环）；**网关七层显式绑回环时第 3 档也不成立**（`webListenLoopback`，排在第 3 档之前：`127.0.0.1:18444` 与 `:18444` 在报文里分得开，登记地址 + 自报端口只对通配监听成立，回环绑定 + 已登记照样不就绪、不签票，且拒绝文案不许说「请登记接入地址」——照做仍不通）；**两栏都登记且不同值 = 判不出来**（剖面把两个都发给终端逐个试拨，浏览器只收一个 302；写死"内网栏优先"时外网用户按钮照亮、跳内网主机名转圈而两侧零报错，出路是配统一入口/资源级 webEntry，或两栏填同一域名）。**门户就绪逐磁贴算**（`PortalTile.web`）：第 1 档是资源级 `webEntry`，用空资源算的全局结论里它永远不可达——照着 503 提示配好 webEntry 却仍被置灰，是本波引入过的回退。**主机可达性判据只有 `store.ClassifyHost` 一处**（登记接口/剖面告警/七层共用）：多认出 `localhost.`、`::1%lo0`，`127.1`/`2130706433` 这类 inet_aton 短写归"不可判定"→ 不就绪（刻意不实现 inet_aton）。**多网关共用同一入口 host 不受支持**（会话 Cookie 是每台网关本机密钥，无粘性会话必断），别拿"票据不绑 gw"去修它。
- **默认安全开局（wave8 行动 16）**：`BAIDI_SEED_MUST_CHANGE` **默认 1**（`deploy.sh` 与 `install-remote.sh` 两处缺省必须一致，否则「手工装」与「按脚本装」得到两种安全姿态而机器上完全同形）；**演示机在 `config.env` 里显式置 0**，`install-remote.sh` 在关闭时打醒目告警。判据同「收口默认值与逃生舱」：默认值就是绝大多数部署的真实姿态，而种子口令 `baidi@123` 是公开的。**限流配置产品自己发**（`deploy/nginx/baidi.conf`：三条登录端点 20r/m、`/api/` 30r/s、`/downloads/` `limit_conn 4`；片段抽成 `baidi-proxy-api.inc`——**不能叫 .conf**，`conf.d/*.conf` 会被 include 进 `http{}` 而它全是 location 级指令，落成 .conf 会炸掉整台机器的 nginx，含共存的烛龙），`build.sh` 构建期自检删一条即红。**别写成「登录可被随便爆破」**：默认部署下 lockout 两维已防住，这份配置真正覆盖的是①免认证大文件下载 `/downloads/`（`ServeFile` 直发几十 MB + `proxy_buffering off`，成本在连接寿命上，故限连接数不限速率）②已认证 API 零配额（FR-INT-16 的真实读法）③管理员手动关掉 IP 维度之后的登录面。
- **入口与实现同口径 / 截断必须可见（wave8 行动 17）**：IPSec 站点 `peer` **拒收 FQDN**——`baidi-ipsec` 的 `parsePeer` 刻意不解析 DNS（抖动会造成谁也解释不清的间歇性故障，`sync_test.go` 把「拒收」钉住），而入口此前放行、400 文案还主动推荐 `sh.example.com`，管理员照填拿 200 OK 后站点安静地永远 down；拒绝要**说得出原因**（笼统的"格式不对"会让人反复换写法试）。`/jit/grants` 与 `/posture` 两处 `LIMIT 500`（`store.ListLimit`）现在回 `total`/`limit`/`truncated`，两页各显示「共 N 条、本页只显示最近 M 条」——**`total` 必须是库里的行数而不是 `len(已读)`**（后者恒等于 500，`truncated` 永远 false，提示等于白加）。判定面不受这道上限影响（告警走 `ActiveGrants`/`StaleGrants`、准入走 `PostureUsersByDisposal` 的 DISTINCT），**别把它写成安全缺陷**。
- **闲置账号治理（FR-MON-19，wave9）**：策略落 `settings`（`store.IdlePolicy`：`thresholdDays` 7~3650 + `autoLock`，**出厂 90 天且不自动锁定**），`GET/PUT /api/v1/users/idle/policy`（读=任意管理员，写=`PermSecurity`，与批量锁定同权）。改造前阈值只从 URL 参数取（刷新一次就回默认 90）、`autoLockEnabled` 整项不存在，PRD 那句「若开启自动锁定，Then 该账号被自动锁定」**没有任何执行方**。执行方是 `StartIdleLockLoop`（`BAIDI_IDLE_LOCK_INTERVAL`，默认 1h；间隔只决定"多久查一次"，**要不要锁由落库策略决定**）。三条纪律：自动锁定与手工批量**共用 `lockIdleAccount` 一条路径**（防自锁 + 管理员目标闸 + 数据面撤窗，「自动」不是绕开守卫的第二条路）· **永不处置管理员账号**（后台没有调用方可比对 `PermAdmins`，能自锁管理员的定时任务最坏把系统锁到没人登得进去）· 管理员跳过**不逐条记审计**（每小时一轮，一个长期不登录的审计管理员就够把审计冲成噪声）。阈值夹取只有 `store.ClampIdleDays` 一处——识别清单与后台锁定必须用同一个值，各夹各的会让页面列出 N 个、后台锁另一批。
- **「在线」在控制台上只有一个判据**：网关上报的真实会话（`api.onlineAccounts()`，与 `GET /online` 同源）。**用户与角色页的 `online/risk/device/ip` 四列与用户状态页的在线绿点都曾各有一套**——前者是 `users` 表里建号那一刻 INSERT 的值（全仓无 UPDATE），后者是 `posture 上报在 10 分钟内`（那是**采集器还活着**，不是"此刻连着隧道"）。三页现同源；取不到一律降级成 `—`/`unknown`，**绝不保留库里那个陈年好值**（从未上报过环境的账号显示「无风险」是替一台完全未知的机器背书）。`DirUser.Auth` 同理改为按实算（口令来源 + 真注册过的 passkey/TOTP），种子里那些「SAML SSO / 密码+UKey / 密码+短信」白帝一种都没实现。
- **控制台的失败必须转述后端原话**：`api.ts` 的 `failReason(e)` / `failStatus(e)` 是唯一收口。全仓曾有 ~35 处 `} catch { Message.error('请检查权限或后端连接') }`——后端那句「最后一名超级管理员不可禁用」「角色「审计管理员」无权执行该操作（需要权限：security）」被整句丢掉换成一个**猜的归因**，管理员照着去查网络、去重登。另有十几处 `msg.startsWith('403')` 是**死分支**（`api()` 抛的是后端中文原文，永远不以状态码开头）。构建期守卫 `console/scripts/check-dead-ui.mjs` **规则三**拦住 bare catch + 编造归因（规则一拦装饰性搜索框、规则二拦死占位）。
- **`GET /api/v1/auth/me` 是控制台身份的唯一来源**：下发 `displayName` + **现算的** `adminRole{key,name,power,perms,scope}`（`currentAdminRoleQuiet`，与 `requirePerm` 同一取数口——角色塞进令牌会在 8h 里冻住）。**只有完整会话令牌才下发角色**（`c.Use == ""`）：受限改密令牌在 `auth.pwResetAllowed` 里对这个端点是放行的，那是连自己口令都还没换的半程态。前端 `lib/me.ts` 的 `permKnown` 区分「判不出来」与「确定无权」，`can()` 判不出来时 **fail-open**（真闸在后端；少显示一个按钮却没有任何报错，比多显示一个更难查）。
- **用户目录的改与删（wave9）**：`PUT /api/v1/users/{id}` 只收姓名与邮箱，**account 显式拒改**（它是令牌主体 JWT Sub，也是 JIT 授予 / 封禁名单 / posture / 用户组成员 / 认证源绑定的关联键，改它会让这些关系整段挂空且不报错）；`DELETE /api/v1/users/{id}` 是 **License 席位的唯一释放路径**（此前 `license.go` 的 409 文案与闲置治理弹窗都指向一条不存在的路），带影响面回执与 `…/delete-preview`。删除做**账号维度**连带清理（认证源绑定 / 用户组成员 / passkey / TOTP / 授信终端 + posture / 接入会话），**刻意不动 `resources.allow_users` 与 `jit_grants`**：前者是安全授权，删账号顺手改别的资源的授权面属于越权，且清掉之后再也看不出这个资源原来授权过谁；后者留给审计（那些授予确实发生过）。两者都写进回执由人去清。防自锁与 `guardAdminTarget` 与置状态同一道闸。**身份源选项卡**同批接真（`DirUser.SourceID`，判据与选项卡计数同源的绑定表；用子查询而非 JOIN——一个账号在多源里有绑定时 JOIN 会把它复制成多行）。
- **外部目录账号不得提升为管理员（wave9）**：`handleCreateAdmin` 的 found 分支与 `handleSetAdminRole` 共用 `guardLocalCredentialForAdmin`。理由是管理台登录**没有认证域路由**（只查本地 bcrypt 哈希），而外部账号 `pass_hash` 恒空——提上去他 200 OK 出现在管理员表里、角色状态一应俱全，却**永远登不进管理台**，且每试一次都被记成「口令错误」并计入防爆破（连门户也一起锁）。选**收敛**而不是打通（与 PRD FR-ADMIN-16「默认本地密码认证」一致），拒绝时说清补救路径：先给他重置一个本地口令。
- **升级包签名（wave9，FR-UPG-04）**：发布方离线 CLI `control/cmd/baidi-upgrade`（`-genkey` / `-manifest` 自动算 SHA-256 / `-sign` / `-verify`）。**签的是 manifest 文件的原文字节**，重新序列化会让签名对不上（症状是"签得好好的包，导入时说签名无效"）。与 `baidi-license` 同一条分界：私钥留在发布方，控制面只配 `BAIDI_UPGRADE_PUBKEY`（可逗号分隔多把供轮换）。**不配 = 校验恒不通过**（fail-closed 是对的），升级页据 `signKeysConfigured` **预先**置灰并说清怎么配，而不是让人先撞一次墙。
- **主机侧定期自动备份（wave9，NFR-AVL-04）**：`BAIDI_BACKUP_DIR`（空=不启用）+ `BAIDI_BACKUP_PASSPHRASE`（**不给默认值**——编译进二进制的默认口令等于没加密）+ `BAIDI_BACKUP_INTERVAL`（默认 24h）+ `BAIDI_BACKUP_KEEP`（默认 7）。**原样复用** `backupSources` + `upgrade.CreateBackup`，绝不另造格式；临时文件 + rename **原子切换**（直接写目标名的话，进程被杀会留下一个与完整备份同名、有大小、时间也新的半截文件）；备份 0600；清理按**文件名**排序而不是 mtime（mtime 会被 rsync / 冷备复制改写，那时删掉的可能正是最该留的那份）。**未配置时 `/diag` 判 warn 而不是 skip**——与 checkNAT 刻意相反：那是"没用这个功能"，而"没有任何自动备份"是每套部署都该关心的事。改造前 `CreateBackup` 只有两个非测试调用方（手点导出直接 stream 给浏览器、备机来拉时现造），**单机部署一份自动备份都没有**。
- **告警规则播种按种类幂等（wave9）**：`seedAlertRules` 原先靠一个全局一次性标记，标记一旦落下**后来新增的规则种类就再也不会被播种**——升级后那一类在告警页上根本不存在、永远不会触发，而页面上看不出缺了什么（「补列迁移必须配回填」的规则表版本）。现在逐种类判断；管理员**主动删掉**的种类记进 `alert.rules.removed.v1`，不会被下次启动复活。
- 演示口令：管理台 admin/baidi@123；门户任意用户+baidi@123。
- **二次认证 = WebAuthn/passkey + TOTP**（门户与管理台都覆盖，管理页 /portal/security）。判定顺序即安全语义（`api.secondFactor`）：**passkey > TOTP > 认证策略**，前两者已注册即无条件强制、策略豁免碰不到。passkey 由 `BAIDI_WEBAUTHN_RPID`+`BAIDI_WEBAUTHN_ORIGIN` 驱动，**RP ID 必须是可注册域名或 localhost——浏览器规范不允许裸 IP**，故 IP 演示站（101.43.125.131）启用不了；**TOTP（`internal/totp` 自研 RFC 6238，SHA1/6 位/30s/±1 步，RFC 官方向量钉住）不依赖域名，是裸 IP 部署下唯一标准二因子**，也是桌面/移动 C/S 客户端唯一能走的二因子（WebAuthn 仪式在客户端里做不了，两端登录已接 `needTotp`）。密钥经 secret 盒加密落 `totp_secrets`（AAD=`totp:`+account），**注册须验证码确认才生效、确认码的时间计数器一并落库**；防重放执行点 `store.ConsumeTotpCounter`（同一 30s 步长的码只能成功一次，判定与更新同一条 UPDATE）；**解绑须出示当前验证码**（拿到会话 ≠ 拿到认证器）。TOTP 登录第二回合 `POST /api/v1/auth/totp` 凭 mfaTicket（与 webauthn 断言同款、免 Bearer 但 handler 强校验）。legacy 演示验证码 123456 仅在「RP 未配置**且**未注册 TOTP」时可达——注册 TOTP 后该回落对本账号从密码学上不可达。认证策略抽屉的二次方式多选由 `authpolicy.SecondaryMethods` 能力声明驱动：**只有 totp 可选，sms/radius/cert/http 置灰+保存拒收+迁移清洗存量**（与 geoAnomaly/winDomain 同一条冻结纪律）。
- **认证策略真接进登录链路**（`internal/authpolicy` 纯函数判定 + `api/authpolicy.go` 取数）：策略按**用户目录**分组（目录由登录链路当场得知：本地哈希命中=local，外部源命中=该源 kind），适用范围用 `scopeOrgs/scopeGroups` 匹配（复用 `store.SubjectIndex`，与资源授权同一处子树展开）。真接线的规则只有五条——增强 `always`（取代此前写死的「账号名 ext*/含外包」启发式，改为按组织/用户组配置）、`weakPwd`（判据是 `users.pw_strength`，**只能在改密/建号那一刻判**，存量行回填 `unknown` 且不命中）、`offHours`（服务器时间 + 策略里的工作日/时段）；豁免 `trustedDevice`（登录带的设备指纹以本账号上报过 posture 且最新判定 allow；指纹客户端自报、**只降低认证要求绝不放宽授权**）、`trustedNetwork`（`clientIP` 比对策略网段，开启必配 CIDR）。**`geoAnomaly` / `winDomain` 判不了（无 IP 地理库、无域校验），已冻结**：保存接口拒绝开启、控制台按后端下发的 `capabilities` 置灰并说明原因、迁移回填清掉存量 true 值；`oneClick` 从模型与 UI 删除（`one_click` 列冻结）。**「主认证方式」下拉与「默认授权应用」已整体删除、PC/移动端两栏已合并、`Secondary` 拿到唯一执行方（wave8 行动 13-②③）**：主认证是**同义反复**而不是"没接线"——`Match` 第一步就按目录筛，一条策略只作用于已经被该目录认出来的人，对他说"主认证用证书"不可能生效；`AuthzApps` 零执行方且把空值渲染成「不授权」，授权的唯一真相是资源侧主体清单 + JIT；**PC/移动端两栏合并**是因为 `loginCtx` 里根本没有端标识（三端走同一个 `/portal/login`），两栏并排会让人以为「移动端能配得更严」——落库的 `pc`/`mobile` 两列保留，**读时并集、写时 pc 存全量 / mobile 存空**（旧控制面读到的仍是可用配置，且无损、不需要一次会漏跑的迁移）。**`AuthPolicy.Secondary` 的唯一执行语义**：策略要求二次认证、而该账号既无 passkey 也无 TOTP 时，**列了方式的策略不接受 legacy 演示验证码回落**（回「请先注册」）——裸 IP 演示站正是它唯一生效的地方；留空 = 不额外约束，行为与改造前逐字一致。**它不决定用哪个因子**（那由已注册的认证器决定）。**求值顺序即安全语义：已注册 passkey 的强制断言排在策略之前——策略只能加强不能削弱**，任何豁免都碰不到它（有测试钉住）。判定材料读不到时 fail-closed 拒登录；决策（含"因 X 豁免"）一律写审计 category=auth。**目录候选由后端下发**（`GET /authpolicy` 的 `directories` = 本地 ∪ 已配置认证源的 kind ∪ 存量策略已用的目录）：登录链路把 `Directory` 置成真实源的 kind，前端写死一份的话 LDAP/OIDC 源永远绑不出一条能命中的策略（Match 按目录先筛一刀，静默）。保存时校验 `directory` 与 `scopeOrgs/scopeGroups` 引用存在（与资源授权共用 `validateSubjectRefs`）；**被策略引用的组织/用户组拒删 409**——删掉即静默停掉一条二次认证策略（fail-open 方向；资源授权的引用是 fail-closed，刻意不拦）。
- **认证域路由（wave8 行动 12，FR-AUTH-09/24）**：登录带 `directory`（外部源 id），**命中即只问该源**。改造前遍历全部 enabled 源逐个 `Authenticate`——接第二个源之后，A 目录员工的**明文口令**会被真实投递到排在前面的每一台 LDAP 服务器去 simple bind（本地口令输错那次也算）。**核心不变式：一次登录只把口令交给一台服务器**（`routeDirectory` 返回值长度恒 ≤1，`ensureDirectoryContext` 是它的可执行表述，只有测试调、别当死代码删）。四档：显式命中→只问它 / 指定未命中→**拒绝**（静默回退到"问全部"正是要消灭的外溢）/ 未指定且单源→问它（单目录部署与老客户端完全不受影响）/ 未指定且多源→拒绝并带回候选（不计入爆破锁定，用户没输错任何东西）。**按 id 匹配不按 kind**——两条 ldap 源的 kind 一样，按 kind 等于在两台之间随机挑一台，外溢只是变得更隐蔽。**显式判定必须排在「一个可用源都没有」之前**，否则点了个被停用的域会一路走到「用户名或密码错误」。免认证端点 `GET /api/v1/auth/domains` 只在 ≥2 个源时回非空、只含 id/name/kind（有用例断言不泄露 host/baseDn）。
- **外部身份准入闸（wave8 行动 10，FR-AUTH-22/FR-USER-13/14）**：认证源配置加 `admitPolicy`（auto|approval，默认 auto 向后兼容，入口校验枚举）+ `allowedDomains`/`allowedGroups`。**两道闸的判定时机刻意不同**：过滤闸**每次登录都判**（目录侧移出组后下次登录即被拒；只判首次的话"移出组"永远不生效），审批闸**只判首次建号**（否则老用户天天要批）。域与组是 **AND**（用 OR 的话"再加一道白名单"反而放宽）；**判不了一律拒**（配了域白名单但没返回邮箱 → fail-closed）。**闸必须在 `BindExternalUser` 之前**，有端到端用例钉住（被拒时 `users` 表一行不多）——判得再对，接在建号之后账号照建。准入拒绝走独立错误类型 `admitDenied`：**不计入爆破锁定**（口令是对的）、文案分「等批准/被拒」、不重复记「口令错误」审计。待批(`fail`)与拒绝(`deny`)在审计里分得开；待批只在**新建单子**那次记（登录可无限重试）。准入登记独立表 `ext_admissions`（不往设备形状的 `approvals` 里塞），但同步生成一条 `approvals.kind=extuser` 让管理员只有一个收件箱；**`kind` 列是必需的**——没有它，设备处置路径会把准入单标成 approved 而人仍进不来，既有行回填 `device`（否则升级那刻所有待批设备都批不了）。**批准不建号**，账号在该用户下次登录时才建。建号与拒绝都落审计（改造前建号完全不留痕）。
- **态势总览按时间窗聚合（wave8 行动 9）**：`Overview(ctx, windowHours)`（`?hours=`，钳制只在 `store.ClampOverviewWindow` 一处，默认 24h、上界 90 天）。**审计派生统计与攻击源共用同一个窗口**——此前前者是全表累计、后者写死 24h，两个口径并排显示在标着「实时判定态势」的同一屏上且一处不标，而留存轮转一到期那个"累计"还会无缘由地往下掉。**窗口对哪些数不生效必须逐条标出**（`DefenseLine.Scope` ∈ `window|current`）：三条防线只有隐身防线真按窗口算，账号防线读 `users` 当前状态、终端防线读 `posture_reports` 最新一份（没有历史）——**一个悄悄不生效的筛选比没有筛选更坏**。窗口超过 `BAIDI_AUDIT_RETENTION_DAYS` 时 `WindowNote` 当面写「实际只覆盖最近 N 天」。窗口的名字只有一个（`humanWindow` 与页面选择器逐字一致，攻击源面板标题也跟随）。TOP 3→5（`store.OverviewTopN`）。顺带：`RecordAudit` 对空 `Time` 补服务端当前时间——空 `ts` 对所有时间窗查询不可见（`ts >= cutoff` 恒假）却会被留存轮转删掉（`ts < cutoff` 恒真），查不到但会消失。
- **放行也留痕（wave8 行动 8，FR-AUDIT-01/02/05 + FR-MON-04）**：三个点——控制面签敲门令牌（`api.auditKnockIssued`，`access`/`allow`，行为人=账号本人）、网关隧道路由命中（`tunnel-allow`）、L7 会话建立（`web-allow`），后两者走 `secevent.ReportAllow` → 心跳 `sec-allow` → 控制面 `verdict=allow`。**全部 5min 节流**（敲门是 15s 保活热路径，不节流一台终端一天 5700 条同样的审计）。**放行的节流键是 (账号,资源) 不是源 IP**（同一人从同一 IP 访问三个资源是三件事，按 IP 折叠会抹掉两件，而那正是 FR-AUDIT-05 要查的维度）；敲门那条的键含设备指纹（只按账号的话第二台机器的首次接入被第一台压掉）。**放行与拒绝各自独立节流**（键带 `+` 前缀）——共用键的话一条放行会把紧随其后的拒绝压掉 5 分钟。**放行绝不进攻击源统计**（只 `verdict=allow`，不调 `RecordAttack`）。数据面事件的 `src_ip` 改记**网关报来的那个**（`api.auditDataplane`），此前一律记 `clientIP(r)`=网关自己的地址，按 src_ip 检索审计永远找不到攻击者。**撤销回执只在「真切断了什么」或「首次封禁」时报**（`revokeReportSet`）：控制面对 disabled/locked 账号滚动续期下发，网关按 `until` 去重必然每轮判成新窗口，一个被禁账号每 15s 一条「撤销 0 个源IP、切断 0 条隧道」的审计（一天 5760 条）；处置照旧每轮执行，改的只是回执，且**解禁后再被封要能重新报**（否则第二次封禁在审计里不存在）。
- **内核态隐身是回执不是断言（wave8 行动 7）**：`darkfw.Probe()` 实测 `nft list table inet baidi` / `pfctl -a baidi-gw -t baidi_allowed -T show` → 随心跳上报 → `api.stealthReceiptOf` 折算成**八态**（`unreported`/`off`/`no-ruleset`/`no-drop-rule`/`orphan-ruleset`/`port-mismatch`/`unknown`/`armed`），网关页逐台呈现 + `/diag checkStealth` 同源。**只有 `armed` 计入生效**——不可判定与未上报都不算。**判据绝不能用 `darkfw.Available()`**：它只查二进制在不在 PATH 上，几乎所有 Linux 都装了 nft，等于写死 true。**默认 DROP 保护的端口要与网关 `-proxy` 比对**（setup 脚本 `PROXY_PORT` 默认 18443，网关换端口后规则集保护的是别人，隧道口照样全世界可见）；**规则集在、却找不到那条 DROP 规则 = `no-drop-rule`（计入 fail）**，绝不能落进 armed——那是「没有任何东西在丢包」，比写死更坏（它带着「实测在位」的措辞）；同一形态在网关启动期解引用 `*GuardedPort` 会 panic，必须先判 nil。**「探不到」优先于一切确定结论**：非 root 时 `Ruleset` 恒 nil，此时哪怕 `-pf` 没开也只能报不可判定并写出两种相反的可能（可能没规则集=端口 open，可能装着=全员连不上），此前直接断言「未启用、端口 open」在参考部署形态下方向与后果两句全反。`/diag` 汇总要把「确定未启用」与「不可判定」分开数。**`Probe` 在没开 `-pf` 时照样探**，为的是抓 `orphan-ruleset`——规则集装着 + 没带 `-pf` = 放行集合永远为空 = **全员连不上**，而敲门成功、控制台显示在线、客户端只是拨号超时。**参考部署默认不开 `-pf`**（`baidi-gateway.service` 明写；strongSwan-gm 也用 nft 需先评审 ruleset），此时未敲门的 TCP **会完成三次握手**再被 `proxy.go` 的 accept-then-close 断开，nmap 判 **open**——页面上那四条「攻击面 = 0」已改为跟随 `allArmed` 渲染，`install-remote.sh` 装完也当面说明。控制面**仍不做外部扫描**，`armed` 的提示里明写「建议从外网侧实测」。
- **数据面安全事件上报（FR-MON-05 + FR-AUDIT-02）**：网关三个面的**每种拒绝**（SPA 敲门 5 种 / L4 隧道 5 种 / L7 Web 10 种，类别枚举与中文名唯一定义在 `store.AttackCatZh`）经 `gateway/internal/secevent` **按 (类别,源IP) 5min 节流**（仿 auditGrayObserved；SPA 收任意 UDP，不节流的洪泛会把 64 条心跳队列冲成全噪声）→ `cplane.QueueSecEvent`（Event 扩 `src/cat/count` 三个机读字段，omitempty 旧控制面无感）→ 心跳捎带 → 控制面落审计 **verdict=deny**（此前一律硬编码 ok，「网关报告了一次拒绝」被数成允许）+ `attack_sources` 小时桶累加（累加的是聚合 count 不是行数）。**节流器内存有界**（4096 键，伪造源洪泛折叠进「（多源聚合）」）；语义是"第一现场立即报、窗口内累计、到期补报"，计数不丢只聚合。安全概览第一格从 trusted_devices 台账顶包换成**隐身防线**（攻击源 TOP）+「SPA 攻击源（24h）」面板（来源数/次数/逐时趋势/TOP5），Memory 后端无攻击表 → `Overview.Attack` 为 nil → 面板不画，**绝不造种子攻击**。留存 `BAIDI_ATTACK_RETENTION_DAYS`（默认 30，无"关闭清理"档）。未配 -control 时 Reporter 不 Bind 保持空转，拒绝仍落本机日志。采集器 `gateway/internal/sysstat`（**纯标准库**：Linux 读 /proc、darwin 走 sysctl+路由套接字；解析函数无 build tag，好让 Linux 的 /proc 格式在 mac 上也能编译+单测）→ 随 mTLS 心跳的 `metrics` 字段上报 → `gateway_metrics` 表 → 监控中心「设备状态」页。**采不到的指标一律「不可判定」（报文缺席 / 落库 NULL / 页面「—」），绝不补 0**——补 0 会让「CPU 0%」伪装成一台空闲机器，并让「CPU>80%」告警对一台失明的网关永久沉默（与 posture 的 unknown 同一条纪律；macOS 上 CPU 恒不可判定，取时间片要 cgo）。**双向兼容有测试**：旧网关不带 metrics → 不落点、单列成「在线但未上报指标」；新网关带 `{}` → 落一条全 NULL 的点，两者可区分。**留存上限 `BAIDI_METRICS_RETENTION_HOURS`（默认 72，没有"关闭清理"这一档）**——它是全系统唯一的高频写入口（每网关 15s 一条），主键 `(gateway_id, ts)` + `INSERT OR REPLACE` 顺带把写入速率钉在每网关每秒一行。趋势按 `range=hour|day|week` **在 SQL 里降采样**（桶宽 60/900/3600s），**空桶不返回**（掉线段断线而非零线），**当前值取最新一条原始采样而不是桶均值**。列名是 `gateway_id` 不是 `gw_id`：与 `ipsec_sa_state` 同名，也是告警 `gateway_load` 规则里 `GatewayMetricsProbe` 读的那个名字，对不上它就永远不触发。
- **灰度链三处收尾（wave8 行动 15）**：①控制台灰度弹窗此前保存恒发 `groups: []` 而 `SaveGrayPlan` 是**整条覆盖式保存**——只改一下比例就把经 API 配好的用户组定向清空（接口回 200、页面看不出差别，灰度对象从「测试组」变成「全体 N% 随机分桶」）；现在弹窗有真多选、`openGray` 回填、保存原样带回。②`upgrade.Coverage` 接进 `/upgrade`（`coverage` 每平台**精确**命中数 + `total` 分母 + `groups` 候选）——**分母是目录全部账号**（只数在线的会让同一条计划在不同时刻显示不同覆盖数，而分桶是确定性的，那种跳动会被读成"灰度不稳定"），**读取失败字段缺席、页面显示「—」不是 0**（画成 0 人管理员会去调高比例）；弹窗预览的措辞与算法一致——定向部分精确、比例部分标明估算（前端算不出服务端 SHA-256 分桶）。③移动端接上 `GET /client/update`（后端按 platform 分桶本就支持 android/ios/harmony，此前 `grep client/update` 在 `clients/mobile` 零命中 = 灰度对移动端完全不可见），版本号改由 vite define 从 `package.json` 注入（手写的必然与真实打包版本分家，而它是更新检查与合规判定的输入）。新增**现场终端实际版本分布**（`store.ClientVersionStats`，`posture_reports` 按 (平台,版本) 聚合）——**「未上报」单列一桶**，并进具体版本会让"有一批机器没报过版本"这件事消失。
- **业务告警（真实体，非流水）**：`alert_rules` + `alerts` 两表，状态机 `pending|ignored|handled`，控制台「监控中心 → 业务告警」。**八类触发源全部读真实存在的信号**（网关心跳超时 / `gateway_metrics` 水位 / JIT 授予将到期 / JIT 授予过期未回收 / 应用未关联资源 / 爆破锁定 / posture 判 block / **审计链周期自检**），出处逐条写在 `store.alertKindSpecs.Signal` 并原样展示在页面上——**不许为不存在的信号建规则**。判定是纯函数 `alerting.Evaluate`（吃信号快照吐候选：条件写反在集成环境里与"一切正常"无法区分，只有纯函数测得住），取数在 `api.alertSnapshot`，落库去重在 `store.RaiseAlert`。**冷却按 (规则, 对象) 且只看时间不看状态**：少了对象键，三台网关同时离线只剩一条；按状态放宽则一点「已处理」就立刻再冒一条。数据源未就绪（如 `gateway_metrics` 无数据）**如实回「等待数据面上报」+ 原因**，不做永不触发的死规则。通知复用消息通道（规则 `channels` 留空=全部启用通道，点名不存在的通道保存即拒；发失败 / 通道停用 / 通道已删三种情况都落审计）。读=任意管理员（角色现算），写=`PermSecurity`。侧栏角标同批接成真实计数（此前写死 '10'/'2'），取不到就不显示，在线用户角标只在 `source=live` 时给数字。
- 未起后端时 console 各页降级为内置演示数据，UI 完整可点（**「设备状态」与「业务告警」两页刻意例外**：连不上就说连不上，不画假曲线、不编假告警——编造的曲线/待办与真实采集在那两页上无法区分，而它们的存在意义正是"有没有异常"）。

## 坑

- gateway/ 根目录 tracked 了两个 13MB 预编译二进制 baidi-tun(.exe)——是历史提交的产物非源码（源码在 gateway/cmd/baidi-tun/），别当文本处理也别轻易删。
- **Windows 的 wintun.dll 必须与 `baidi-tun.exe` 同目录**：wintun 用 `LoadLibraryEx(APPLICATION_DIR|SYSTEM32)` 加载，只看**发起加载的进程自身 exe 目录**与 System32——不看 PATH、不看 cwd。DLL 不入库，`src-tauri/fetch-wintun.sh` 构建期从官方取件 + SHA-256 强校验，`build-sidecars.sh` 在 GOOS=windows 时自动调。**取件地址只有 `https://www.wintun.net/builds/wintun-<版本>.zip` 一条**（旧文档里的 `git.zx2c4.com/wintun/builds/…` 是 404）；**哈希是官网公布值人工逐字符比对来的，不是"把下载到的文件算一遍填进去"**（那等于给任何一次投毒盖章），故 **改版本号必须同一次改掉哈希**——`WINTUN_VERSION`/`WINTUN_URL`/`WINTUN_SHA256` 三常量刻意挨着写就是为让这个疏漏无处可藏，忘了也不会静默（校验当场拒）。版本号全仓只此一处。离线/内网用 `BAIDI_WINTUN_ZIP=<本地 zip>`，它**照样过同一道强校验**，不是跳过校验的开关。升级清单（含"必须重读新版 LICENSE"）见 clients/BUILD.md 第四节；随包分发的依据是 Wintun 预编译许可第 3(d) 条例外，义务只有两条——**不改 DLL / 不从中剥版权注记**（3(a)(b)(c)）与**不借其名号背书**（3(e)）。**随包附 `wintun-LICENSE.txt` 是我们自愿的，许可原文并未要求**（3(c) 管的是"不得从 DLL 这个二进制里移除版权注记"，别把它写成许可的硬性要求——那句话会随包发到用户手里）；既然说了会附就有执行方：`verify-wintun-stage.sh` 打包前查、CI 打包后查。**NSIS `installMode` 必须是 `perMachine`**：Tauri 缺省的 `currentUser` 装进 `%LOCALAPPDATA%`（用户可写），而那个目录里的 `wintun.dll` 与 `baidi-tun.exe` 随后被 UAC 提权加载/执行，等于给普通权限进程留了一条提权路径；构建期哈希钉扎只覆盖构建机上的 zip，覆盖不到用户机上那份，产物也**未签名**。**`wintun.dll.triple` 必须有消费方**：它记的是固定位那份 DLL 的架构，`build-sidecars.sh` 与 `beforeBundleCommand` 两处调 `verify-wintun-stage.sh` 复核——只写不读的话，手工跑过一次取件就能把 arm64 的 DLL 打进 x64 包且全程零报错。打包配置在 **`tauri.windows.conf.json`**（平台专属，放主配置会让 mac/Linux 构建失败），且 `bundle.resources` **必须写成映射形 + 目的地空串**——列表形会保留 `binaries/wintun/` 目录层级装进子目录，包照样打得出、装得上、文件也在，只在用户点「接入」那一刻炸。守卫有三道：Rust 单测钉住配置写法、CI 打包前验 PE machine 与 runner 架构一致、CI 打包后解析生成的 `installer.nsi` + `msiexec /a` 摊开 MSI 断言 DLL 与 `baidi-tun.exe` 父目录同一。运行期 `elevate.rs::preflight_start` 在**弹 UAC 之前**查存在性与架构（三态：读不出/认不出→放行）。
- `design-system/` 是烛龙黏土橙**遗留目录**（fork 残留），白帝不消费它——改主题只动 console/src/styles/tokens.css。
- **烛龙共存契约**：nginx 站点绝不允许 default_server（build.sh/install-remote.sh 有自检，检出即中止）；deploy/wipe-remote.sh + WIPE=1 会铲目标机原有业务，慎开。
- certs/（SM2 双证 pem，含私钥）已进 .gitignore——是本地 gmca 产物，任何情况下不入库。
- Go 版本不一致：control 要 go 1.25，gateway 要 go 1.26.3；交叉编译全程 CGO_ENABLED=0。
- curl 不支持国密 TLCP，验证 -gm 隧道用 gateway/cmd/baidi-tlcp-probe。
- 重置数据：删 control/baidi.db 重启即重灌种子。
- **补列迁移必须配回填**：`addColumnIfMissing` 只加列不填值，既有库的新列永久为 NULL。`apps.resource_id` 就这么静默断过——应用↔资源桥接为空 → JIT 解析不出资源、客户端剖面排不出路由，两处都**无报错**。加业务语义列时同步写回填（见 `backfillAppResourceID`）。
- **IPSec 已真实实现，但边界很硬**：纯 Go IKEv2+ESP（`gateway/internal/ipsec/`，独立进程 `baidi-ipsec`）。**只支持 PSK 认证、ESP 只走 UDP-4500 封装、未与 strongSwan 实机互通验证过**；`suite=gm` 用 IANA 私有码点，只白帝↔白帝，**不是国密 IPSec，别这么说**。toggle 改的是 `enabled`（管理意图），`state` 由网关实测回报——`ipsec_sites` 的 `status/rx_bytes/tx_bytes/last_up` 四列已废弃，读运行态一律走 `ipsec_sa_state`。站点 `gatewayId` **必须以 `ipsec-` 开头**（它就是组网网关 mTLS 证书的 CN，控制面按 CN 精确过滤下发；前缀不对时网关拉到空列表而非错误，站点安静地永远 down）。其余真伪边界见 docs/ARCHITECTURE.md 第七节。
- **IKE_AUTH 之外的交换必须先过已认证闸**（`ike.onRequest`）：SK_e/SK_a 只由 DH + nonce + SPI 派生，PSK 只进 AUTH 载荷——任何能完成一次 IKE_SA_INIT 的对端都持有可用的 SK 密钥。少了这道闸，不知道 PSK 的对端跳过 IKE_AUTH 直发 CREATE_CHILD_SA 就能装进一条真通的 ESP 隧道，而 `States()` 因 `primary()` 要求 SAEstablished 仍不显示 up（数据面已通、控制台无痕）。判据必须用 `State.authenticated()` 而非 `Keys != nil`——**密钥就绪不等于身份已验**。
- **展示值必须来自「数据面真正在用的那份」**：桌面客户端接入信息取 `tunnel.ts` 的 `startedOpts` 快照而非当前剖面。全局剖面随时会被刷新（切「应用」页即重拉），而 baidi-tun 的参数在拉起那一刻定死——现算当前剖面会把**未钉扎的运行中隧道显示成已钉扎**。同理 `profile.error` 与剖面 `warnings` 必须在「接入」页渲染：剖面拉不到会退回本机默认配置（单网段、无 resmap、无 pin）却仍显示「已接入」。
