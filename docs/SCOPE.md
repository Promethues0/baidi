# 白帝范围边界 · 烛龙 PRD 22 章逐章取舍

来源 PRD：《烛龙零信任访问控制系统 — PRD V1.0》（由深信服 aTrust v2.5.16 手册逆向推导）。
白帝沿用该 PRD 的零信任**访问控制**主线，对 UEM 与安全中心管理模块做减法。图例：✅ 保留并复用烛龙 · ➖ 部分裁剪 · ❌ 不做。

| PRD 章 | 模块 | 白帝 | 落地 / 说明 |
|---|---|:--:|---|
| S / 1 | 执行摘要 · 产品概述与定位 | ✅ | 复用；定位收窄为"零信任访问控制系统" |
| 2 | 安装与部署 | ✅ | 分离式 / AIO 形态复用烛龙引擎；console 无专页 |
| 3 | 系统架构与总体设计 | ➖ | 控制面/数据面分离原则不变；**网关横向扩展 + 客户端故障转移（FR-ARCH-03/04）已真实现**：剖面下发有序落点清单（在线优先→id 字典序，离线也下发）、指纹逐网关、客户端按序切换且切换在接入页可见，单数 `gateway` 字段保留供旧客户端。**控制面自身的多活/选主不做**（SQLite 单写者，双写会静默丢配置）；控制面侧改为**温备已真实现**（`baidi-standby` 周期拉加密备份 → 校验 → 落盘 → 回报，主机侧 mTLS `standby-` 前缀分权，系统页/`/diag` 三态真判定，切换脚本 `deploy/promote-standby.sh` 且干跑有用例跑）——RPO = 同步间隔、切换需人工触发、不做自动选主（两节点无仲裁必然脑裂），**就近选择与负载均衡刻意不做**（没有地理/时延数据；心跳指标无调度语义，按 CPU 选最闲会把所有新连接一起打到同一台）；边界见 docs/ARCHITECTURE.md 第七节 |
| 4 | 产品升级管理 | ➖ | 升级前校验（版本序/禁降级/强制链路/组件一致性/包签名）、加密配置备份；**客户端灰度 = 服务端判定真实现 + 终端提示闭环**：控制面按 SHA-256(平台\|账号) 稳定分桶 + 定向账号名单，且只在目标版本**高于**终端上报版本时才回 `update=true`（相等或更低不提示——那是降级）；**终端两端都接了最后一跳**（桌面端登录/重开即查、移动端「我的」页进入即查 `GET /client/update`，有新版出横幅指向门户下载中心，检查失败静默不报错——后端按 platform 分桶本来就支持 android/ios/harmony，此前只是移动端从没调过）。**灰度链已收尾（wave8 行动 15）**：按用户组定向在控制台可配可回读（此前弹窗保存恒发 `groups: []`，管理员只要在页面上改一下比例就会把经 API 配过的用户组无声清空——**一个会吞配置的页面不属于"已知限制"，那是缺陷**）；`upgrade.Coverage` 接进升级页，每条计划显示**精确**命中人数（分桶确定性，不是 `accounts×percent/100` 的估算），读取失败显示「—」而不是 0；新增**现场终端实际版本分布**（`posture_reports` 按 (平台,版本) 聚合，「未上报」单列一桶），那是 AC-12「先小范围验证再放开」在真机上的唯一验收依据。**仍不做**：客户端不自动下载、不自动安装、不自动回滚，装包一律人工去下载中心取；灰度只影响"告诉谁有新版"，不影响任何人**实际**装了什么版本（唯一权威事实是 posture 上报的 `client_version`）。服务端换二进制仍由部署脚本执行，集群升级编排未做（集群本就没部署） |
| 5 | 监控中心 | ✅ | `MonitorDashboard` 安全监控大屏 |
| 6 | 用户与角色（身份目录） | ✅ | `IdentityUsers/Org/Groups`；**外部目录组映射已真**（LDAP memberOf / OIDC groups → kind=external 用户组，按登录逐次收敛、双源隔离、页面只读，直接进 `SubjectIndex` 供资源授权与认证策略引用）；目录全量同步为 L 级延后项（见 docs/charter/wave7.md D 组）；**明示不做**：自定义 HTTPS 认证目录（FR-USER-05）——私有认证服务器没有稳定 Subject，绑定只能退回按用户名，那正是本项目在认证源实现里指认过的冒充漏洞（AD 里新建一个同名 admin 即可冒充本地管理员）；企业微信/钉钉/浙政钉/飞书目录连接器（FR-USER-08 后半）——依赖外部平台租户与实机验证，本环境无法诚实交付，标准路径是这些平台的 OIDC 出口（已可用） |
| 7 | 认证管理 | ✅ | `IdentityAuth/AuthPolicy/Idp/PwdPolicy/SecPolicy/Waiver`；**外部账号状态回验已真，但覆盖面按目录类型分三档，别笼统说**（wave8 行动 11 校准）：**AD** = 禁用（userAccountControl 位）/ 过期（accountExpires）/ 删除 / 移出 BaseDN，四种都识别，无需配置；**通用 LDAP** = 删除 / 移出 BaseDN 恒识别，「禁用」**要配了状态属性映射才识别**（`statusAttr` + `statusDisabledValues`，控制台给了 IDTrust/389DS/OpenLDAP ppolicy 三个预设）——LDAP 协议里没有统一的「禁用」语义，不配就只剩存在性这一种证据，页面当面告警；**OIDC** = **没有周期回验**，OIDC 协议不提供状态查询通道（那是 SCIM 的职责）。OIDC 账号在**每次登录**时由 IdP 天然重新校验（被停用的人完不成授权码流），缺的只是**会话中途**吊销，窗口上界 = 会话有效期。刻意不做 refresh_token 轮询：那要把一把能替用户向 IdP 换令牌的长效凭据落到本地库，库一旦泄露，攻击面比它想解决的问题大得多。要覆盖中途吊销请缩短会话有效期，或给同一目录再配一条 LDAP 源做回验。三档共同的纪律：源不可用（网络/绑定失败）绝不动手，只单向禁用不自动恢复；**明示不做**：LDAP 手机号字段映射（FR-AUTH-08 子项）——短信网关已明拒，手机号在系统内没有任何消费方，映射进来即孤儿数据。**认证策略抽屉同批收口（wave8 行动 13-②③）**：删掉两端的「主认证方式」下拉（策略按目录筛，对已被该目录认出来的人说"主认证用证书"是同义反复）与「默认授权应用」（零执行方），合并 PC/移动端两栏（三端走同一个 `/portal/login`，请求里没有端标识），并给 `secondary` 一个真执行方——列了方式的策略**不接受 legacy 演示验证码回落**，裸 IP 演示站正是它唯一生效的地方 |
| 8 | 应用管理（资源发布） | ➖ | 隧道应用发布/资源鉴权/JIT 审批已真实现；**七层 Web 代理（8.3.3）已真实现**（控制面签 use=web 短时效一次性票据 → 网关 L7 验票换会话 Cookie → 逐请求重新鉴权 → 反代；XFF 按真实对端重写、Location/Set-Cookie 作用域改写、`web-e2e.sh` 九条断言，含票据重放、跨应用 Cookie、撤权后立即失效三条反例）。仍不做：**HTML 正文绝对链接改写**（无底洞，如实标边界）、**DLP/水印/禁复制打印下载**（属 ch11 UEM，整章不做）、**SSO 免认证代填**；L7 端口不受 SPA 隐身保护，边界见 docs/ARCHITECTURE.md 第七节。**应用可改可删已补齐（wave8 行动 14，FR-APP-01 P0）**：`PUT/DELETE /api/v1/apps/{id}`——改造前只有 GET+POST，而控制台「编辑」走的是发布向导 → POST，点一次多出一条同名应用；下架**不级联删受控资源**（可能被别的应用引用、也可能挂着 JIT 授予），回执与确认弹窗都当面说明。顺带摘掉 `apps.node`（管理员没有这个输入项，一律写死「华东出口」）。**「WEB 全网资源」正名为「直连书签」**：`mode=global` 不经网关、不受访问控制、对全体登录用户可见，三处名字统一 + 向导与编辑抽屉各挂告警 + 门户打开行为改对（此前提示「请用桌面客户端接入后访问」，而它压根不走隧道）；**泛域名代理（证书签发 + 正文改写）仍不做** |
| 9 | 终端管理 | ✅ | `IdentityDevices` + `IdentityCompliance`（终端合规基线 = 设备 posture，属身份/设备信任，**保留**）；**资产分类与标签（FR-EP-06~09）已真实现**：`asset_class`(企业/个人/企业纳管个人) 是准入判据，执行方落在敲门令牌闸的 (账号,指纹) 粒度上（`personalPolicy` = 跟随全局/按严格判/一律拒，`managed` 按企业资产处理）——**刻意不并入账号维度的风险降权**，否则同一个人的企业机会被他的个人机连坐；`tags` 只是台账属性、无执行方且页面当面标明。**边界**：真正的「按设备区分能访问哪些资源」需要把指纹贯穿数据面（撤销通道与网关 DenyUsers 现在都只有账号维度），本波只做到"这台设备能不能拿到敲门令牌"；**记档延后（非否决）**：终端日志远程收集（FR-EP-17/18/19）——需要新造服务端→客户端的指令下发通道（现架构客户端只拉不收），改造面大于收益；桌面端本地一键诊断报告落地后价值进一步降低 |
| 10 | 策略管理（全局/用户策略） | ➖ | **接入策略两条 P0 已真实现**（wave8 行动 13-①）：FR-POLICY-29 同时在线设备上限（0~1000，0=禁止接入，可区分 PC/移动分别计数，名额先到先得所以调小上限会真的把最晚接入的挤掉）与 FR-POLICY-30 接入超时注销（无**业务流量**超时，5 分钟~365 天）。执行点是**敲门令牌**（`api.accessSessionGate`），撤销在一个 15s 保活周期内生效；活跃时刻来自网关逐会话回执（`spa.Allowlist.Touch` → 心跳 `lastActive`），**敲门保活刻意不算活跃**，否则这条规则永远不会触发。防爆破（FR-POLICY-06/07）真实现见 ch7。**摘除的**：「用户策略 · 继承编辑器」那 8 项（设备并发数/会话空闲超时/专用 DNS/虚拟专线隔离/登录时段/二次认证豁免期/卸载防护/进程防护）——它们落进 `policy_overrides.settings` 后**全仓零消费方**，而保存提示写着「已下发至代理网关」，影响预览里的平台分布是 `members×0.62/0.16` 编的。**不做**：按组织/用户组分级的策略继承（FR-POLICY-02~05，接入策略当前是全局的）、PC 端「无键鼠操作」超时（控制面拿不到终端输入事件）、卸载/进程防护（需驱动或系统服务）、弱网/0RTT 优化、禁止浏览器登录、强制安装/升级客户端——逐条理由列在策略页「本版本未实现」里。**PolicySimulator / LoginFlowSim 从未实现**，规划页未建 |
| **11** | **UEM 统一终端数据安全** | ❌ | **整章不做**：移动/PC 数据安全、沙箱工作空间、外发审批等 DLP 移出范围 |
| **12** | **安全中心** | ➖ | **安全基线管理**：**保留并真实现**（此前这一行写着「砍」，与代码相反——`console/src/views/Security.vue` 带完整 CRUD，且 `baseline_policies` 是风险引擎 `internal/risk` 的**唯一规则源**，砍掉它等于终端合规判定没有规则）。基线的检测项 key 受采集器目录约束、`disposal` 四档都有执行方、**适用范围（wave8 行动 13-④）已结构化并真接进判定**（`scopeOrgs`/`scopeGroups`，组织含子树，与资源授权/认证策略共用 `store.SubjectIndex`；两栏都空 = 对全体生效）；同批摘掉无人读的 `type`（上线准入/应用防护）与自由文本 `scope`。**真砍掉的是**：虚拟网络域 / 可信应用 / 独立的终端基线管理页（终端合规并进 ch9）。**SPA 服务隐身保留**为安全代理网关内建能力（隐身价值主张底座，烛龙 console 本就不暴露专页，天然对齐），其真实态回执见 wave8 行动 7 |
| **13** | 深信服设备联动与第三方设备管理 | ➖ | 深信服专有联动（EDR/AC 私有协议）不做；**保留开放 API/标准化第三方联动**（见 ch21） |
| 14 | 主动防御（欺骗防御 / 恶意识别） | ➖ | **仅处置末端真实**（强制下线/账号冻结/JIT 撤销真落数据面）；安全雷达/蜜罐诱捕/恶意识别未实现，规划页未建 |
| 15 | 系统管理 | ➖ | 三权分立 / 消息通道 / 日志外送 / 加密配置备份 / 运维诊断均已真实现；**HA（15.5）落成温备而非双活**：备机拉加密备份+校验+落盘，不提供服务，切换由 `deploy/promote-standby.sh` 人工触发，RPO = 同步间隔。**时间一致性已真实现**（网关心跳自报本机时钟 → 控制面收包时刻做差 → 网关页三态呈现（数值/未上报，绝不把未上报当 0）+ `clock_skew` 告警规则 + `/diag` 时钟检查，阈值挂着敲门令牌有效期这个真实临界点；**NTP 客户端配置刻意不做**——时钟同步由部署机chrony/ntpd 自治，控制面只做检测与告警，代管 NTP 等于给控制面加一个能改全网时间的写权）。**运营报表已真实现**（`GET /api/v1/audit/report`：audit_log/alerts 的 SQL 聚合——逐日分桶补零日、留存截断如实标记、登录/被拒账号榜单、告警三档；**归 PermAudit** 且挂在 /api/v1/audit* 前缀下——报表是聚合过的审计正文，聚合并不脱敏，给 security/system 开这个口等于三权分立只剩字面成立；页面无演示数据，连不上如实说）。**License 已真实现**（Ed25519 发行签名：发行方离线 CLI `baidi-license` 持私钥，控制面只配公钥 `BAIDI_LICENSE_PUBKEY`、未配置即拒导入；容量执行点收在建号（/users、/admins 建号分支）与签发网关证书两处硬闸，换证/提权/吊销不误占席位；demo=不限、invalid/expired=fail-closed 拒新增；外部认证源建号刻意不硬拦——拦在登录链路是随机拒绝服务，超限走状态页亮红；**不是防拷贝**，边界如实写在页面上） |
| 16 | 审计中心（日志） | ➖ | 写操作实时落库已真（45+ 审计点，admin 门 + XFF 信任边界）；HMAC-SM3 防篡改链（/audit/verify 全链校验）、留存轮转（BAIDI_AUDIT_RETENTION_DAYS，链锚点接续）、CSV 流式导出均已真；**日志外送已真**（RFC 5424 syslog over TCP/TLS + HTTP JSON，持久化队列 + 退避重试 + 队列上界与丢弃计数，外送带 seq/mac 供 SIEM 独立验真；刻意不做 UDP）；**明示不做**：解密流量旁路镜像（FR-AUDIT-16/17）——那个形态依赖硬件化网关与专用镜像口，与白帝的进程形态不匹配；SIEM 深度审计的需求已由带 seq/mac 的审计外送承接（SIEM 侧可独立验真，那是旁路镜像给不了的） |
| 17 | IPSec VPN 组网 | ✅ | `GatewayIpsec` 站点编排（复用烛龙 IPSEC 引擎） |
| 18 | 地址转换（NAT） | ✅ | SNAT/DNAT 策略建模 + 校验 + 编译成内核 nft/pf 规则并下发；隧道/敲门流量自动排除、SPA 互斥当面告警。真实报文转换需 root + 多网卡实机，未实测（同 darkfw 边界） |
| 19 | 典型部署场景 | ➖ | 文档级为主。多数据中心那一半的**客户端侧**已真实现（多网关落点 + 故障转移，见 ch3）；跨中心的统一入口/智能 DNS 就近解析不做 |
| 20 | 非功能性需求（NFR） | ✅ | 安全/性能/可靠/信创/合规，贯穿；**明示不做**：SNMP（NFR-OPS-03/OBS-03）——网关指标 + 业务告警 + syslog/SIEM 外送已覆盖可观测性主诉求；将来确有 NMS 生态需求，再评估只读暴露 gateway_metrics，而不是补一套 MIB |
| 21 | 集成、开放能力与数据模型 | ✅ | 认证源 / SSO / 开放平台 API/SDK / 日志外送 |
| 22 | 需求优先级与路线图 | ✅ | 白帝自有路线图（后续补 `docs/charter`） |

## Console IA（6 中心 + 系统管理，40 页全保留）

> ⚠️ 下面是**规划 IA**（自烛龙沿袭的目标形态），不是现状。实际落地的导航见 `console/src/nav.ts`：4 组 20 页，页名与此处的 `MonitorDashboard/IdentityUsers` 等规划名不一一对应。以代码为准。

```
概览     安全监控大屏 · 运行总览
身份中心  用户 · 终端设备 · 组织树 · 动态组 | 认证方式&MFA · IdP 联邦 | 认证策略 · 口令策略 · 账号安全 · 认证豁免 · 终端合规基线
资源中心  统一资源对象 · 资源审批 · 应用门户编排 | 地址/服务/时间对象
策略中心  统一策略 · 策略仿真器 · 登录流仿真 | 主动防御 · 关联规则 · IP 信誉名单
网关中心  网关清单 · SSL 接入点 · Mesh 中继/连接器 · IPSec 站点编排
审计中心  统一事件 · 审计链（HMAC-SM3）
系统管理  证书与密钥 · License · 管理员权限 | 日志外发 · 告警通知 · 时间&NTP · HA · 备份恢复 · 运营报表 | 系统诊断
```

无 UEM 中心、无安全中心管理页——白帝 IA 与"去 UEM / 去安全中心管理"边界天然一致。
