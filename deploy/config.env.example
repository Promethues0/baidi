# 复制为 config.env 并填写后用于 deploy.sh（config.env 已 gitignore）
# 目标服务器 SSH（参照烛龙部署机 124.223.225.77）
SERVER_SSH=root@124.223.225.77
PUBLIC_HOST=124.223.225.77

# 安装前缀 / 运行用户 / 控制中心内部端口 / CORS 来源
BD_PREFIX=/opt/baidi
BD_USER=baidi
CONTROL_PORT=8090
PUBLIC_ORIGIN=*
